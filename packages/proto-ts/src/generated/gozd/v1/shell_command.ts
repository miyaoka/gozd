// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: gozd/v1/shell_command.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "gozd.v1";

export interface ShellCommandInstallRequest {
}

export interface ShellCommandInstallResponse {
  /** symlink 配置先（`~/.local/bin/gozd`） */
  source: string;
  /** symlink が指す先（.app 内 wrapper の絶対パス） */
  target: string;
  /** 既に同じ target を指す symlink が存在し、何もしなかった */
  alreadyInstalled: boolean;
  /** 別の target を指していた symlink を上書きした */
  replaced: boolean;
}

export interface ShellCommandUninstallRequest {
}

export interface ShellCommandUninstallResponse {
  source: string;
  /** 削除した（存在し、かつこの .app の wrapper を指していた） */
  removed: boolean;
  /** 既に存在しなかった */
  notInstalled: boolean;
}

function createBaseShellCommandInstallRequest(): ShellCommandInstallRequest {
  return {};
}

export const ShellCommandInstallRequest: MessageFns<ShellCommandInstallRequest> = {
  encode(_: ShellCommandInstallRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShellCommandInstallRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShellCommandInstallRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ShellCommandInstallRequest {
    return {};
  },

  toJSON(_: ShellCommandInstallRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ShellCommandInstallRequest>): ShellCommandInstallRequest {
    return ShellCommandInstallRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ShellCommandInstallRequest>): ShellCommandInstallRequest {
    const message = createBaseShellCommandInstallRequest();
    return message;
  },
};

function createBaseShellCommandInstallResponse(): ShellCommandInstallResponse {
  return { source: "", target: "", alreadyInstalled: false, replaced: false };
}

export const ShellCommandInstallResponse: MessageFns<ShellCommandInstallResponse> = {
  encode(message: ShellCommandInstallResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.source !== "") {
      writer.uint32(10).string(message.source);
    }
    if (message.target !== "") {
      writer.uint32(18).string(message.target);
    }
    if (message.alreadyInstalled !== false) {
      writer.uint32(24).bool(message.alreadyInstalled);
    }
    if (message.replaced !== false) {
      writer.uint32(32).bool(message.replaced);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShellCommandInstallResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShellCommandInstallResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.target = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.alreadyInstalled = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.replaced = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShellCommandInstallResponse {
    return {
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      target: isSet(object.target) ? globalThis.String(object.target) : "",
      alreadyInstalled: isSet(object.alreadyInstalled)
        ? globalThis.Boolean(object.alreadyInstalled)
        : isSet(object.already_installed)
        ? globalThis.Boolean(object.already_installed)
        : false,
      replaced: isSet(object.replaced) ? globalThis.Boolean(object.replaced) : false,
    };
  },

  toJSON(message: ShellCommandInstallResponse): unknown {
    const obj: any = {};
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.target !== "") {
      obj.target = message.target;
    }
    if (message.alreadyInstalled !== false) {
      obj.alreadyInstalled = message.alreadyInstalled;
    }
    if (message.replaced !== false) {
      obj.replaced = message.replaced;
    }
    return obj;
  },

  create(base?: DeepPartial<ShellCommandInstallResponse>): ShellCommandInstallResponse {
    return ShellCommandInstallResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ShellCommandInstallResponse>): ShellCommandInstallResponse {
    const message = createBaseShellCommandInstallResponse();
    message.source = object.source ?? "";
    message.target = object.target ?? "";
    message.alreadyInstalled = object.alreadyInstalled ?? false;
    message.replaced = object.replaced ?? false;
    return message;
  },
};

function createBaseShellCommandUninstallRequest(): ShellCommandUninstallRequest {
  return {};
}

export const ShellCommandUninstallRequest: MessageFns<ShellCommandUninstallRequest> = {
  encode(_: ShellCommandUninstallRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShellCommandUninstallRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShellCommandUninstallRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ShellCommandUninstallRequest {
    return {};
  },

  toJSON(_: ShellCommandUninstallRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ShellCommandUninstallRequest>): ShellCommandUninstallRequest {
    return ShellCommandUninstallRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ShellCommandUninstallRequest>): ShellCommandUninstallRequest {
    const message = createBaseShellCommandUninstallRequest();
    return message;
  },
};

function createBaseShellCommandUninstallResponse(): ShellCommandUninstallResponse {
  return { source: "", removed: false, notInstalled: false };
}

export const ShellCommandUninstallResponse: MessageFns<ShellCommandUninstallResponse> = {
  encode(message: ShellCommandUninstallResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.source !== "") {
      writer.uint32(10).string(message.source);
    }
    if (message.removed !== false) {
      writer.uint32(16).bool(message.removed);
    }
    if (message.notInstalled !== false) {
      writer.uint32(24).bool(message.notInstalled);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShellCommandUninstallResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShellCommandUninstallResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.removed = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.notInstalled = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShellCommandUninstallResponse {
    return {
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      removed: isSet(object.removed) ? globalThis.Boolean(object.removed) : false,
      notInstalled: isSet(object.notInstalled)
        ? globalThis.Boolean(object.notInstalled)
        : isSet(object.not_installed)
        ? globalThis.Boolean(object.not_installed)
        : false,
    };
  },

  toJSON(message: ShellCommandUninstallResponse): unknown {
    const obj: any = {};
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.removed !== false) {
      obj.removed = message.removed;
    }
    if (message.notInstalled !== false) {
      obj.notInstalled = message.notInstalled;
    }
    return obj;
  },

  create(base?: DeepPartial<ShellCommandUninstallResponse>): ShellCommandUninstallResponse {
    return ShellCommandUninstallResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ShellCommandUninstallResponse>): ShellCommandUninstallResponse {
    const message = createBaseShellCommandUninstallResponse();
    message.source = object.source ?? "";
    message.removed = object.removed ?? false;
    message.notInstalled = object.notInstalled ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
