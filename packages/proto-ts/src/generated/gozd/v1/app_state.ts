// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: gozd/v1/app_state.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "gozd.v1";

/**
 * アプリ全体の状態。`~/.config/gozd/app-state.json` に永続化される。
 *
 * 永続化形式は proto3 JSON mapping。SwiftProtobuf の jsonString() で
 * 直接ファイルに書き、jsonString() で復元する。RPC ワイヤーフォーマットと
 * 永続化形式を同一 proto 型で揃え、Codable との二重管理を避ける。
 */
export interface AppState {
  windowFrame:
    | WindowFrame
    | undefined;
  /** 最後に開いていた worktree の絶対パス（再起動時の復元用） */
  lastOpenedDir: string;
  /** window 内に同居するサイドバー repo リスト。順序は dirOrder と一致する */
  sidebarRepos: SidebarRepo[];
}

/**
 * サイドバー上の 1 repo の永続化エントリ。worktrees / freeBranches は
 * 起動時に再 fetch するため永続化しない（一次情報は git に問い合わせる）。
 */
export interface SidebarRepo {
  rootDir: string;
  repoName: string;
  isGitRepo: boolean;
  collapsed: boolean;
}

export interface WindowFrame {
  x: number;
  y: number;
  width: number;
  height: number;
}

export interface LoadAppStateRequest {
}

export interface LoadAppStateResponse {
  state: AppState | undefined;
}

export interface SaveAppStateRequest {
  state: AppState | undefined;
}

export interface SaveAppStateResponse {
}

function createBaseAppState(): AppState {
  return { windowFrame: undefined, lastOpenedDir: "", sidebarRepos: [] };
}

export const AppState: MessageFns<AppState> = {
  encode(message: AppState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.windowFrame !== undefined) {
      WindowFrame.encode(message.windowFrame, writer.uint32(10).fork()).join();
    }
    if (message.lastOpenedDir !== "") {
      writer.uint32(18).string(message.lastOpenedDir);
    }
    for (const v of message.sidebarRepos) {
      SidebarRepo.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.windowFrame = WindowFrame.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.lastOpenedDir = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sidebarRepos.push(SidebarRepo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppState {
    return {
      windowFrame: isSet(object.windowFrame)
        ? WindowFrame.fromJSON(object.windowFrame)
        : isSet(object.window_frame)
        ? WindowFrame.fromJSON(object.window_frame)
        : undefined,
      lastOpenedDir: isSet(object.lastOpenedDir)
        ? globalThis.String(object.lastOpenedDir)
        : isSet(object.last_opened_dir)
        ? globalThis.String(object.last_opened_dir)
        : "",
      sidebarRepos: globalThis.Array.isArray(object?.sidebarRepos)
        ? object.sidebarRepos.map((e: any) => SidebarRepo.fromJSON(e))
        : globalThis.Array.isArray(object?.sidebar_repos)
        ? object.sidebar_repos.map((e: any) => SidebarRepo.fromJSON(e))
        : [],
    };
  },

  toJSON(message: AppState): unknown {
    const obj: any = {};
    if (message.windowFrame !== undefined) {
      obj.windowFrame = WindowFrame.toJSON(message.windowFrame);
    }
    if (message.lastOpenedDir !== "") {
      obj.lastOpenedDir = message.lastOpenedDir;
    }
    if (message.sidebarRepos?.length) {
      obj.sidebarRepos = message.sidebarRepos.map((e) => SidebarRepo.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<AppState>): AppState {
    return AppState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AppState>): AppState {
    const message = createBaseAppState();
    message.windowFrame = (object.windowFrame !== undefined && object.windowFrame !== null)
      ? WindowFrame.fromPartial(object.windowFrame)
      : undefined;
    message.lastOpenedDir = object.lastOpenedDir ?? "";
    message.sidebarRepos = object.sidebarRepos?.map((e) => SidebarRepo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSidebarRepo(): SidebarRepo {
  return { rootDir: "", repoName: "", isGitRepo: false, collapsed: false };
}

export const SidebarRepo: MessageFns<SidebarRepo> = {
  encode(message: SidebarRepo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rootDir !== "") {
      writer.uint32(10).string(message.rootDir);
    }
    if (message.repoName !== "") {
      writer.uint32(18).string(message.repoName);
    }
    if (message.isGitRepo !== false) {
      writer.uint32(24).bool(message.isGitRepo);
    }
    if (message.collapsed !== false) {
      writer.uint32(32).bool(message.collapsed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SidebarRepo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSidebarRepo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rootDir = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.repoName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isGitRepo = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.collapsed = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SidebarRepo {
    return {
      rootDir: isSet(object.rootDir)
        ? globalThis.String(object.rootDir)
        : isSet(object.root_dir)
        ? globalThis.String(object.root_dir)
        : "",
      repoName: isSet(object.repoName)
        ? globalThis.String(object.repoName)
        : isSet(object.repo_name)
        ? globalThis.String(object.repo_name)
        : "",
      isGitRepo: isSet(object.isGitRepo)
        ? globalThis.Boolean(object.isGitRepo)
        : isSet(object.is_git_repo)
        ? globalThis.Boolean(object.is_git_repo)
        : false,
      collapsed: isSet(object.collapsed) ? globalThis.Boolean(object.collapsed) : false,
    };
  },

  toJSON(message: SidebarRepo): unknown {
    const obj: any = {};
    if (message.rootDir !== "") {
      obj.rootDir = message.rootDir;
    }
    if (message.repoName !== "") {
      obj.repoName = message.repoName;
    }
    if (message.isGitRepo !== false) {
      obj.isGitRepo = message.isGitRepo;
    }
    if (message.collapsed !== false) {
      obj.collapsed = message.collapsed;
    }
    return obj;
  },

  create(base?: DeepPartial<SidebarRepo>): SidebarRepo {
    return SidebarRepo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SidebarRepo>): SidebarRepo {
    const message = createBaseSidebarRepo();
    message.rootDir = object.rootDir ?? "";
    message.repoName = object.repoName ?? "";
    message.isGitRepo = object.isGitRepo ?? false;
    message.collapsed = object.collapsed ?? false;
    return message;
  },
};

function createBaseWindowFrame(): WindowFrame {
  return { x: 0, y: 0, width: 0, height: 0 };
}

export const WindowFrame: MessageFns<WindowFrame> = {
  encode(message: WindowFrame, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.x !== 0) {
      writer.uint32(9).double(message.x);
    }
    if (message.y !== 0) {
      writer.uint32(17).double(message.y);
    }
    if (message.width !== 0) {
      writer.uint32(25).double(message.width);
    }
    if (message.height !== 0) {
      writer.uint32(33).double(message.height);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WindowFrame {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWindowFrame();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.x = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.y = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.width = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.height = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WindowFrame {
    return {
      x: isSet(object.x) ? globalThis.Number(object.x) : 0,
      y: isSet(object.y) ? globalThis.Number(object.y) : 0,
      width: isSet(object.width) ? globalThis.Number(object.width) : 0,
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
    };
  },

  toJSON(message: WindowFrame): unknown {
    const obj: any = {};
    if (message.x !== 0) {
      obj.x = message.x;
    }
    if (message.y !== 0) {
      obj.y = message.y;
    }
    if (message.width !== 0) {
      obj.width = message.width;
    }
    if (message.height !== 0) {
      obj.height = message.height;
    }
    return obj;
  },

  create(base?: DeepPartial<WindowFrame>): WindowFrame {
    return WindowFrame.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WindowFrame>): WindowFrame {
    const message = createBaseWindowFrame();
    message.x = object.x ?? 0;
    message.y = object.y ?? 0;
    message.width = object.width ?? 0;
    message.height = object.height ?? 0;
    return message;
  },
};

function createBaseLoadAppStateRequest(): LoadAppStateRequest {
  return {};
}

export const LoadAppStateRequest: MessageFns<LoadAppStateRequest> = {
  encode(_: LoadAppStateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoadAppStateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoadAppStateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): LoadAppStateRequest {
    return {};
  },

  toJSON(_: LoadAppStateRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<LoadAppStateRequest>): LoadAppStateRequest {
    return LoadAppStateRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<LoadAppStateRequest>): LoadAppStateRequest {
    const message = createBaseLoadAppStateRequest();
    return message;
  },
};

function createBaseLoadAppStateResponse(): LoadAppStateResponse {
  return { state: undefined };
}

export const LoadAppStateResponse: MessageFns<LoadAppStateResponse> = {
  encode(message: LoadAppStateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== undefined) {
      AppState.encode(message.state, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoadAppStateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoadAppStateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.state = AppState.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LoadAppStateResponse {
    return { state: isSet(object.state) ? AppState.fromJSON(object.state) : undefined };
  },

  toJSON(message: LoadAppStateResponse): unknown {
    const obj: any = {};
    if (message.state !== undefined) {
      obj.state = AppState.toJSON(message.state);
    }
    return obj;
  },

  create(base?: DeepPartial<LoadAppStateResponse>): LoadAppStateResponse {
    return LoadAppStateResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LoadAppStateResponse>): LoadAppStateResponse {
    const message = createBaseLoadAppStateResponse();
    message.state = (object.state !== undefined && object.state !== null)
      ? AppState.fromPartial(object.state)
      : undefined;
    return message;
  },
};

function createBaseSaveAppStateRequest(): SaveAppStateRequest {
  return { state: undefined };
}

export const SaveAppStateRequest: MessageFns<SaveAppStateRequest> = {
  encode(message: SaveAppStateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== undefined) {
      AppState.encode(message.state, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SaveAppStateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSaveAppStateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.state = AppState.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SaveAppStateRequest {
    return { state: isSet(object.state) ? AppState.fromJSON(object.state) : undefined };
  },

  toJSON(message: SaveAppStateRequest): unknown {
    const obj: any = {};
    if (message.state !== undefined) {
      obj.state = AppState.toJSON(message.state);
    }
    return obj;
  },

  create(base?: DeepPartial<SaveAppStateRequest>): SaveAppStateRequest {
    return SaveAppStateRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SaveAppStateRequest>): SaveAppStateRequest {
    const message = createBaseSaveAppStateRequest();
    message.state = (object.state !== undefined && object.state !== null)
      ? AppState.fromPartial(object.state)
      : undefined;
    return message;
  },
};

function createBaseSaveAppStateResponse(): SaveAppStateResponse {
  return {};
}

export const SaveAppStateResponse: MessageFns<SaveAppStateResponse> = {
  encode(_: SaveAppStateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SaveAppStateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSaveAppStateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): SaveAppStateResponse {
    return {};
  },

  toJSON(_: SaveAppStateResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<SaveAppStateResponse>): SaveAppStateResponse {
    return SaveAppStateResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<SaveAppStateResponse>): SaveAppStateResponse {
    const message = createBaseSaveAppStateResponse();
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
