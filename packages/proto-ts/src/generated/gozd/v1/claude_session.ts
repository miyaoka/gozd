// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: gozd/v1/claude_session.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "gozd.v1";

export interface ClaudeSession {
  worktreePath: string;
  sessionId: string;
  /** ~/.claude/projects/<encoded-cwd>/<session-id>.jsonl への絶対パス */
  transcriptPath: string;
  /** ISO8601。デバッグ用途 */
  updatedAt: string;
}

export interface ClaudeSessionList {
  sessions: ClaudeSession[];
}

/** 指定 dir に紐づく Claude セッション一覧を返す。renderer が worktree オープン時に呼ぶ。 */
export interface ClaudeSessionListByDirRequest {
  dir: string;
}

export interface ClaudeSessionListByDirResponse {
  sessions: ClaudeSession[];
}

/**
 * プロジェクト全体（任意の worktree dir から projectKey 解決）の生存セッション一覧を返す。
 * サイドバーで「各 worktree に何個 resume 可能か」を一括取得するため。
 */
export interface ClaudeSessionListByProjectRequest {
  /** プロジェクト内のいずれかの dir。projectKey 解決に使う。 */
  dir: string;
}

export interface ClaudeSessionListByProjectResponse {
  sessions: ClaudeSession[];
}

function createBaseClaudeSession(): ClaudeSession {
  return { worktreePath: "", sessionId: "", transcriptPath: "", updatedAt: "" };
}

export const ClaudeSession: MessageFns<ClaudeSession> = {
  encode(message: ClaudeSession, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.worktreePath !== "") {
      writer.uint32(10).string(message.worktreePath);
    }
    if (message.sessionId !== "") {
      writer.uint32(18).string(message.sessionId);
    }
    if (message.transcriptPath !== "") {
      writer.uint32(26).string(message.transcriptPath);
    }
    if (message.updatedAt !== "") {
      writer.uint32(34).string(message.updatedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClaudeSession {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClaudeSession();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.worktreePath = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.transcriptPath = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.updatedAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClaudeSession {
    return {
      worktreePath: isSet(object.worktreePath)
        ? globalThis.String(object.worktreePath)
        : isSet(object.worktree_path)
        ? globalThis.String(object.worktree_path)
        : "",
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      transcriptPath: isSet(object.transcriptPath)
        ? globalThis.String(object.transcriptPath)
        : isSet(object.transcript_path)
        ? globalThis.String(object.transcript_path)
        : "",
      updatedAt: isSet(object.updatedAt)
        ? globalThis.String(object.updatedAt)
        : isSet(object.updated_at)
        ? globalThis.String(object.updated_at)
        : "",
    };
  },

  toJSON(message: ClaudeSession): unknown {
    const obj: any = {};
    if (message.worktreePath !== "") {
      obj.worktreePath = message.worktreePath;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.transcriptPath !== "") {
      obj.transcriptPath = message.transcriptPath;
    }
    if (message.updatedAt !== "") {
      obj.updatedAt = message.updatedAt;
    }
    return obj;
  },

  create(base?: DeepPartial<ClaudeSession>): ClaudeSession {
    return ClaudeSession.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ClaudeSession>): ClaudeSession {
    const message = createBaseClaudeSession();
    message.worktreePath = object.worktreePath ?? "";
    message.sessionId = object.sessionId ?? "";
    message.transcriptPath = object.transcriptPath ?? "";
    message.updatedAt = object.updatedAt ?? "";
    return message;
  },
};

function createBaseClaudeSessionList(): ClaudeSessionList {
  return { sessions: [] };
}

export const ClaudeSessionList: MessageFns<ClaudeSessionList> = {
  encode(message: ClaudeSessionList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.sessions) {
      ClaudeSession.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClaudeSessionList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClaudeSessionList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessions.push(ClaudeSession.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClaudeSessionList {
    return {
      sessions: globalThis.Array.isArray(object?.sessions)
        ? object.sessions.map((e: any) => ClaudeSession.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ClaudeSessionList): unknown {
    const obj: any = {};
    if (message.sessions?.length) {
      obj.sessions = message.sessions.map((e) => ClaudeSession.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ClaudeSessionList>): ClaudeSessionList {
    return ClaudeSessionList.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ClaudeSessionList>): ClaudeSessionList {
    const message = createBaseClaudeSessionList();
    message.sessions = object.sessions?.map((e) => ClaudeSession.fromPartial(e)) || [];
    return message;
  },
};

function createBaseClaudeSessionListByDirRequest(): ClaudeSessionListByDirRequest {
  return { dir: "" };
}

export const ClaudeSessionListByDirRequest: MessageFns<ClaudeSessionListByDirRequest> = {
  encode(message: ClaudeSessionListByDirRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dir !== "") {
      writer.uint32(10).string(message.dir);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClaudeSessionListByDirRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClaudeSessionListByDirRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dir = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClaudeSessionListByDirRequest {
    return { dir: isSet(object.dir) ? globalThis.String(object.dir) : "" };
  },

  toJSON(message: ClaudeSessionListByDirRequest): unknown {
    const obj: any = {};
    if (message.dir !== "") {
      obj.dir = message.dir;
    }
    return obj;
  },

  create(base?: DeepPartial<ClaudeSessionListByDirRequest>): ClaudeSessionListByDirRequest {
    return ClaudeSessionListByDirRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ClaudeSessionListByDirRequest>): ClaudeSessionListByDirRequest {
    const message = createBaseClaudeSessionListByDirRequest();
    message.dir = object.dir ?? "";
    return message;
  },
};

function createBaseClaudeSessionListByDirResponse(): ClaudeSessionListByDirResponse {
  return { sessions: [] };
}

export const ClaudeSessionListByDirResponse: MessageFns<ClaudeSessionListByDirResponse> = {
  encode(message: ClaudeSessionListByDirResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.sessions) {
      ClaudeSession.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClaudeSessionListByDirResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClaudeSessionListByDirResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessions.push(ClaudeSession.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClaudeSessionListByDirResponse {
    return {
      sessions: globalThis.Array.isArray(object?.sessions)
        ? object.sessions.map((e: any) => ClaudeSession.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ClaudeSessionListByDirResponse): unknown {
    const obj: any = {};
    if (message.sessions?.length) {
      obj.sessions = message.sessions.map((e) => ClaudeSession.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ClaudeSessionListByDirResponse>): ClaudeSessionListByDirResponse {
    return ClaudeSessionListByDirResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ClaudeSessionListByDirResponse>): ClaudeSessionListByDirResponse {
    const message = createBaseClaudeSessionListByDirResponse();
    message.sessions = object.sessions?.map((e) => ClaudeSession.fromPartial(e)) || [];
    return message;
  },
};

function createBaseClaudeSessionListByProjectRequest(): ClaudeSessionListByProjectRequest {
  return { dir: "" };
}

export const ClaudeSessionListByProjectRequest: MessageFns<ClaudeSessionListByProjectRequest> = {
  encode(message: ClaudeSessionListByProjectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dir !== "") {
      writer.uint32(10).string(message.dir);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClaudeSessionListByProjectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClaudeSessionListByProjectRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dir = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClaudeSessionListByProjectRequest {
    return { dir: isSet(object.dir) ? globalThis.String(object.dir) : "" };
  },

  toJSON(message: ClaudeSessionListByProjectRequest): unknown {
    const obj: any = {};
    if (message.dir !== "") {
      obj.dir = message.dir;
    }
    return obj;
  },

  create(base?: DeepPartial<ClaudeSessionListByProjectRequest>): ClaudeSessionListByProjectRequest {
    return ClaudeSessionListByProjectRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ClaudeSessionListByProjectRequest>): ClaudeSessionListByProjectRequest {
    const message = createBaseClaudeSessionListByProjectRequest();
    message.dir = object.dir ?? "";
    return message;
  },
};

function createBaseClaudeSessionListByProjectResponse(): ClaudeSessionListByProjectResponse {
  return { sessions: [] };
}

export const ClaudeSessionListByProjectResponse: MessageFns<ClaudeSessionListByProjectResponse> = {
  encode(message: ClaudeSessionListByProjectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.sessions) {
      ClaudeSession.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClaudeSessionListByProjectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClaudeSessionListByProjectResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessions.push(ClaudeSession.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClaudeSessionListByProjectResponse {
    return {
      sessions: globalThis.Array.isArray(object?.sessions)
        ? object.sessions.map((e: any) => ClaudeSession.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ClaudeSessionListByProjectResponse): unknown {
    const obj: any = {};
    if (message.sessions?.length) {
      obj.sessions = message.sessions.map((e) => ClaudeSession.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ClaudeSessionListByProjectResponse>): ClaudeSessionListByProjectResponse {
    return ClaudeSessionListByProjectResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ClaudeSessionListByProjectResponse>): ClaudeSessionListByProjectResponse {
    const message = createBaseClaudeSessionListByProjectResponse();
    message.sessions = object.sessions?.map((e) => ClaudeSession.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
