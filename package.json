{
  "name": "gozd",
  "version": "0.0.0",
  "private": true,
  "description": "AI Agent Orchestrator",
  "type": "module",
  "scripts": {
    "dev": "GOZD_DEV_VITE_PORT=16873 concurrently --kill-others --names renderer,native --prefix-colors cyan,magenta 'pnpm --filter @gozd/renderer dev' 'pnpm --filter @gozd/native dev'",
    "dev:prod": "rm -rf \"$TMPDIR/gozd-stable-launch\" \"$TMPDIR/gozd-stable.sock\" && env -u GOZD_SOCKET_PATH apps/native/.build/app/Gozd.app/Contents/Resources/app/bin/gozd",
    "build": "pnpm -r build",
    "bootstrap": "pnpm build && open apps/native/.build/app/Gozd.app",
    "format": "oxfmt .",
    "lint": "oxlint --type-aware",
    "fix": "oxfmt '!apps/**' '!packages/**'",
    "lint:all": "pnpm -r --include-workspace-root --if-present --no-bail --aggregate-output --reporter=append-only lint",
    "fix:all": "pnpm -r --include-workspace-root --if-present --no-bail --aggregate-output --reporter=append-only fix",
    "test:all": "pnpm -r --if-present --no-bail --aggregate-output --reporter=append-only test",
    "typecheck:all": "pnpm -r --include-workspace-root --if-present typecheck",
    "knip": "knip",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "concurrently": "10.0.3",
    "knip": "6.16.0",
    "lefthook": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:"
  }
}
