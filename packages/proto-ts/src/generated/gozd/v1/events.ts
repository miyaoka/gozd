// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: gozd/v1/events.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { OpenTargetSelection } from "./common";

export const protobufPackage = "gozd.v1";

/** fsChange: 任意ディレクトリ配下のファイル変更 */
export interface FsChangeEvent {
  /** 監視 root に対する相対ディレクトリ */
  relDir: string;
}

/** gitStatusChange: アクティブ worktree の git status 変化 */
export interface GitStatusChangeEvent {
  dir: string;
  statuses: { [key: string]: string };
  head: string;
  hasUpstream: boolean;
  ahead: number;
  behind: number;
}

export interface GitStatusChangeEvent_StatusesEntry {
  key: string;
  value: string;
}

/** branchChange: ブランチ ref の変更（作成・削除・リネーム） */
export interface BranchChangeEvent {
  dir: string;
}

/** worktreeChange: 非アクティブ worktree の変更 */
export interface WorktreeChangeEvent {
  dir: string;
}

/** gozdOpen: CLI から `gozd open <path>` 等が来た時に renderer へ通知 */
export interface GozdOpenEvent {
  dir: string;
  selection: OpenTargetSelection | undefined;
  channel: string;
  repoName: string;
  isGitRepo: boolean;
  /** 既存ウィンドウ再利用時に切り替え先を指定 */
  switchToDir: string;
}

/** notify: desktop / cli からの通知 */
export interface NotifyEvent {
  /** "error" / "info" */
  type: string;
  source: string;
  message: string;
  detail: string;
}

function createBaseFsChangeEvent(): FsChangeEvent {
  return { relDir: "" };
}

export const FsChangeEvent: MessageFns<FsChangeEvent> = {
  encode(message: FsChangeEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.relDir !== "") {
      writer.uint32(10).string(message.relDir);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsChangeEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsChangeEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.relDir = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsChangeEvent {
    return {
      relDir: isSet(object.relDir)
        ? globalThis.String(object.relDir)
        : isSet(object.rel_dir)
        ? globalThis.String(object.rel_dir)
        : "",
    };
  },

  toJSON(message: FsChangeEvent): unknown {
    const obj: any = {};
    if (message.relDir !== "") {
      obj.relDir = message.relDir;
    }
    return obj;
  },

  create(base?: DeepPartial<FsChangeEvent>): FsChangeEvent {
    return FsChangeEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FsChangeEvent>): FsChangeEvent {
    const message = createBaseFsChangeEvent();
    message.relDir = object.relDir ?? "";
    return message;
  },
};

function createBaseGitStatusChangeEvent(): GitStatusChangeEvent {
  return { dir: "", statuses: {}, head: "", hasUpstream: false, ahead: 0, behind: 0 };
}

export const GitStatusChangeEvent: MessageFns<GitStatusChangeEvent> = {
  encode(message: GitStatusChangeEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dir !== "") {
      writer.uint32(10).string(message.dir);
    }
    globalThis.Object.entries(message.statuses).forEach(([key, value]: [string, string]) => {
      GitStatusChangeEvent_StatusesEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    if (message.head !== "") {
      writer.uint32(26).string(message.head);
    }
    if (message.hasUpstream !== false) {
      writer.uint32(32).bool(message.hasUpstream);
    }
    if (message.ahead !== 0) {
      writer.uint32(40).uint32(message.ahead);
    }
    if (message.behind !== 0) {
      writer.uint32(48).uint32(message.behind);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GitStatusChangeEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGitStatusChangeEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dir = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = GitStatusChangeEvent_StatusesEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.statuses[entry2.key] = entry2.value;
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.head = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.hasUpstream = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.ahead = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.behind = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GitStatusChangeEvent {
    return {
      dir: isSet(object.dir) ? globalThis.String(object.dir) : "",
      statuses: isObject(object.statuses)
        ? (globalThis.Object.entries(object.statuses) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      head: isSet(object.head) ? globalThis.String(object.head) : "",
      hasUpstream: isSet(object.hasUpstream)
        ? globalThis.Boolean(object.hasUpstream)
        : isSet(object.has_upstream)
        ? globalThis.Boolean(object.has_upstream)
        : false,
      ahead: isSet(object.ahead) ? globalThis.Number(object.ahead) : 0,
      behind: isSet(object.behind) ? globalThis.Number(object.behind) : 0,
    };
  },

  toJSON(message: GitStatusChangeEvent): unknown {
    const obj: any = {};
    if (message.dir !== "") {
      obj.dir = message.dir;
    }
    if (message.statuses) {
      const entries = globalThis.Object.entries(message.statuses) as [string, string][];
      if (entries.length > 0) {
        obj.statuses = {};
        entries.forEach(([k, v]) => {
          obj.statuses[k] = v;
        });
      }
    }
    if (message.head !== "") {
      obj.head = message.head;
    }
    if (message.hasUpstream !== false) {
      obj.hasUpstream = message.hasUpstream;
    }
    if (message.ahead !== 0) {
      obj.ahead = Math.round(message.ahead);
    }
    if (message.behind !== 0) {
      obj.behind = Math.round(message.behind);
    }
    return obj;
  },

  create(base?: DeepPartial<GitStatusChangeEvent>): GitStatusChangeEvent {
    return GitStatusChangeEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GitStatusChangeEvent>): GitStatusChangeEvent {
    const message = createBaseGitStatusChangeEvent();
    message.dir = object.dir ?? "";
    message.statuses = (globalThis.Object.entries(object.statuses ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.head = object.head ?? "";
    message.hasUpstream = object.hasUpstream ?? false;
    message.ahead = object.ahead ?? 0;
    message.behind = object.behind ?? 0;
    return message;
  },
};

function createBaseGitStatusChangeEvent_StatusesEntry(): GitStatusChangeEvent_StatusesEntry {
  return { key: "", value: "" };
}

export const GitStatusChangeEvent_StatusesEntry: MessageFns<GitStatusChangeEvent_StatusesEntry> = {
  encode(message: GitStatusChangeEvent_StatusesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GitStatusChangeEvent_StatusesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGitStatusChangeEvent_StatusesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GitStatusChangeEvent_StatusesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: GitStatusChangeEvent_StatusesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<GitStatusChangeEvent_StatusesEntry>): GitStatusChangeEvent_StatusesEntry {
    return GitStatusChangeEvent_StatusesEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GitStatusChangeEvent_StatusesEntry>): GitStatusChangeEvent_StatusesEntry {
    const message = createBaseGitStatusChangeEvent_StatusesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseBranchChangeEvent(): BranchChangeEvent {
  return { dir: "" };
}

export const BranchChangeEvent: MessageFns<BranchChangeEvent> = {
  encode(message: BranchChangeEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dir !== "") {
      writer.uint32(10).string(message.dir);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BranchChangeEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBranchChangeEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dir = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BranchChangeEvent {
    return { dir: isSet(object.dir) ? globalThis.String(object.dir) : "" };
  },

  toJSON(message: BranchChangeEvent): unknown {
    const obj: any = {};
    if (message.dir !== "") {
      obj.dir = message.dir;
    }
    return obj;
  },

  create(base?: DeepPartial<BranchChangeEvent>): BranchChangeEvent {
    return BranchChangeEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BranchChangeEvent>): BranchChangeEvent {
    const message = createBaseBranchChangeEvent();
    message.dir = object.dir ?? "";
    return message;
  },
};

function createBaseWorktreeChangeEvent(): WorktreeChangeEvent {
  return { dir: "" };
}

export const WorktreeChangeEvent: MessageFns<WorktreeChangeEvent> = {
  encode(message: WorktreeChangeEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dir !== "") {
      writer.uint32(10).string(message.dir);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorktreeChangeEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorktreeChangeEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dir = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorktreeChangeEvent {
    return { dir: isSet(object.dir) ? globalThis.String(object.dir) : "" };
  },

  toJSON(message: WorktreeChangeEvent): unknown {
    const obj: any = {};
    if (message.dir !== "") {
      obj.dir = message.dir;
    }
    return obj;
  },

  create(base?: DeepPartial<WorktreeChangeEvent>): WorktreeChangeEvent {
    return WorktreeChangeEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WorktreeChangeEvent>): WorktreeChangeEvent {
    const message = createBaseWorktreeChangeEvent();
    message.dir = object.dir ?? "";
    return message;
  },
};

function createBaseGozdOpenEvent(): GozdOpenEvent {
  return { dir: "", selection: undefined, channel: "", repoName: "", isGitRepo: false, switchToDir: "" };
}

export const GozdOpenEvent: MessageFns<GozdOpenEvent> = {
  encode(message: GozdOpenEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dir !== "") {
      writer.uint32(10).string(message.dir);
    }
    if (message.selection !== undefined) {
      OpenTargetSelection.encode(message.selection, writer.uint32(18).fork()).join();
    }
    if (message.channel !== "") {
      writer.uint32(26).string(message.channel);
    }
    if (message.repoName !== "") {
      writer.uint32(34).string(message.repoName);
    }
    if (message.isGitRepo !== false) {
      writer.uint32(40).bool(message.isGitRepo);
    }
    if (message.switchToDir !== "") {
      writer.uint32(50).string(message.switchToDir);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GozdOpenEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGozdOpenEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dir = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.selection = OpenTargetSelection.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.channel = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.repoName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.isGitRepo = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.switchToDir = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GozdOpenEvent {
    return {
      dir: isSet(object.dir) ? globalThis.String(object.dir) : "",
      selection: isSet(object.selection) ? OpenTargetSelection.fromJSON(object.selection) : undefined,
      channel: isSet(object.channel) ? globalThis.String(object.channel) : "",
      repoName: isSet(object.repoName)
        ? globalThis.String(object.repoName)
        : isSet(object.repo_name)
        ? globalThis.String(object.repo_name)
        : "",
      isGitRepo: isSet(object.isGitRepo)
        ? globalThis.Boolean(object.isGitRepo)
        : isSet(object.is_git_repo)
        ? globalThis.Boolean(object.is_git_repo)
        : false,
      switchToDir: isSet(object.switchToDir)
        ? globalThis.String(object.switchToDir)
        : isSet(object.switch_to_dir)
        ? globalThis.String(object.switch_to_dir)
        : "",
    };
  },

  toJSON(message: GozdOpenEvent): unknown {
    const obj: any = {};
    if (message.dir !== "") {
      obj.dir = message.dir;
    }
    if (message.selection !== undefined) {
      obj.selection = OpenTargetSelection.toJSON(message.selection);
    }
    if (message.channel !== "") {
      obj.channel = message.channel;
    }
    if (message.repoName !== "") {
      obj.repoName = message.repoName;
    }
    if (message.isGitRepo !== false) {
      obj.isGitRepo = message.isGitRepo;
    }
    if (message.switchToDir !== "") {
      obj.switchToDir = message.switchToDir;
    }
    return obj;
  },

  create(base?: DeepPartial<GozdOpenEvent>): GozdOpenEvent {
    return GozdOpenEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GozdOpenEvent>): GozdOpenEvent {
    const message = createBaseGozdOpenEvent();
    message.dir = object.dir ?? "";
    message.selection = (object.selection !== undefined && object.selection !== null)
      ? OpenTargetSelection.fromPartial(object.selection)
      : undefined;
    message.channel = object.channel ?? "";
    message.repoName = object.repoName ?? "";
    message.isGitRepo = object.isGitRepo ?? false;
    message.switchToDir = object.switchToDir ?? "";
    return message;
  },
};

function createBaseNotifyEvent(): NotifyEvent {
  return { type: "", source: "", message: "", detail: "" };
}

export const NotifyEvent: MessageFns<NotifyEvent> = {
  encode(message: NotifyEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== "") {
      writer.uint32(10).string(message.type);
    }
    if (message.source !== "") {
      writer.uint32(18).string(message.source);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    if (message.detail !== "") {
      writer.uint32(34).string(message.detail);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NotifyEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNotifyEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.detail = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NotifyEvent {
    return {
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      detail: isSet(object.detail) ? globalThis.String(object.detail) : "",
    };
  },

  toJSON(message: NotifyEvent): unknown {
    const obj: any = {};
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.detail !== "") {
      obj.detail = message.detail;
    }
    return obj;
  },

  create(base?: DeepPartial<NotifyEvent>): NotifyEvent {
    return NotifyEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NotifyEvent>): NotifyEvent {
    const message = createBaseNotifyEvent();
    message.type = object.type ?? "";
    message.source = object.source ?? "";
    message.message = object.message ?? "";
    message.detail = object.detail ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
