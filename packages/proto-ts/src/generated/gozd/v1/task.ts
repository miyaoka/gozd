// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: gozd/v1/task.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Task, WorktreeEntry } from "./common";

export const protobufPackage = "gozd.v1";

/** TaskStore の永続化形式（tasks.json）。RPC レスポンスとファイル形式を共有する。 */
export interface TaskList {
  tasks: Task[];
}

export interface TaskListRequest {
  dir: string;
}

export interface TaskListResponse {
  tasks: Task[];
}

export interface TaskAddRequest {
  dir: string;
  body: string;
  worktreeDir: string;
  prNumber: number;
  issueNumber: number;
}

export interface TaskAddResponse {
  task: Task | undefined;
}

export interface TaskUpdateRequest {
  dir: string;
  id: string;
  body: string;
}

export interface TaskUpdateResponse {
  task: Task | undefined;
}

export interface TaskRemoveRequest {
  dir: string;
  id: string;
}

export interface TaskRemoveResponse {
}

/** 1 RPC で task 作成 + worktree 作成を atomic に行う */
export interface CreateWorktreeWithTaskRequest {
  dir: string;
  id: string;
  worktreeDir: string;
  branch: string;
}

export interface CreateWorktreeWithTaskResponse {
  task: Task | undefined;
  worktree: WorktreeEntry | undefined;
  dir: string;
}

function createBaseTaskList(): TaskList {
  return { tasks: [] };
}

export const TaskList: MessageFns<TaskList> = {
  encode(message: TaskList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.tasks) {
      Task.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TaskList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTaskList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tasks.push(Task.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TaskList {
    return { tasks: globalThis.Array.isArray(object?.tasks) ? object.tasks.map((e: any) => Task.fromJSON(e)) : [] };
  },

  toJSON(message: TaskList): unknown {
    const obj: any = {};
    if (message.tasks?.length) {
      obj.tasks = message.tasks.map((e) => Task.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<TaskList>): TaskList {
    return TaskList.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TaskList>): TaskList {
    const message = createBaseTaskList();
    message.tasks = object.tasks?.map((e) => Task.fromPartial(e)) || [];
    return message;
  },
};

function createBaseTaskListRequest(): TaskListRequest {
  return { dir: "" };
}

export const TaskListRequest: MessageFns<TaskListRequest> = {
  encode(message: TaskListRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dir !== "") {
      writer.uint32(10).string(message.dir);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TaskListRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTaskListRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dir = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TaskListRequest {
    return { dir: isSet(object.dir) ? globalThis.String(object.dir) : "" };
  },

  toJSON(message: TaskListRequest): unknown {
    const obj: any = {};
    if (message.dir !== "") {
      obj.dir = message.dir;
    }
    return obj;
  },

  create(base?: DeepPartial<TaskListRequest>): TaskListRequest {
    return TaskListRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TaskListRequest>): TaskListRequest {
    const message = createBaseTaskListRequest();
    message.dir = object.dir ?? "";
    return message;
  },
};

function createBaseTaskListResponse(): TaskListResponse {
  return { tasks: [] };
}

export const TaskListResponse: MessageFns<TaskListResponse> = {
  encode(message: TaskListResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.tasks) {
      Task.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TaskListResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTaskListResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tasks.push(Task.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TaskListResponse {
    return { tasks: globalThis.Array.isArray(object?.tasks) ? object.tasks.map((e: any) => Task.fromJSON(e)) : [] };
  },

  toJSON(message: TaskListResponse): unknown {
    const obj: any = {};
    if (message.tasks?.length) {
      obj.tasks = message.tasks.map((e) => Task.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<TaskListResponse>): TaskListResponse {
    return TaskListResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TaskListResponse>): TaskListResponse {
    const message = createBaseTaskListResponse();
    message.tasks = object.tasks?.map((e) => Task.fromPartial(e)) || [];
    return message;
  },
};

function createBaseTaskAddRequest(): TaskAddRequest {
  return { dir: "", body: "", worktreeDir: "", prNumber: 0, issueNumber: 0 };
}

export const TaskAddRequest: MessageFns<TaskAddRequest> = {
  encode(message: TaskAddRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dir !== "") {
      writer.uint32(10).string(message.dir);
    }
    if (message.body !== "") {
      writer.uint32(18).string(message.body);
    }
    if (message.worktreeDir !== "") {
      writer.uint32(26).string(message.worktreeDir);
    }
    if (message.prNumber !== 0) {
      writer.uint32(32).uint32(message.prNumber);
    }
    if (message.issueNumber !== 0) {
      writer.uint32(40).uint32(message.issueNumber);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TaskAddRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTaskAddRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dir = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.body = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.worktreeDir = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.prNumber = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.issueNumber = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TaskAddRequest {
    return {
      dir: isSet(object.dir) ? globalThis.String(object.dir) : "",
      body: isSet(object.body) ? globalThis.String(object.body) : "",
      worktreeDir: isSet(object.worktreeDir)
        ? globalThis.String(object.worktreeDir)
        : isSet(object.worktree_dir)
        ? globalThis.String(object.worktree_dir)
        : "",
      prNumber: isSet(object.prNumber)
        ? globalThis.Number(object.prNumber)
        : isSet(object.pr_number)
        ? globalThis.Number(object.pr_number)
        : 0,
      issueNumber: isSet(object.issueNumber)
        ? globalThis.Number(object.issueNumber)
        : isSet(object.issue_number)
        ? globalThis.Number(object.issue_number)
        : 0,
    };
  },

  toJSON(message: TaskAddRequest): unknown {
    const obj: any = {};
    if (message.dir !== "") {
      obj.dir = message.dir;
    }
    if (message.body !== "") {
      obj.body = message.body;
    }
    if (message.worktreeDir !== "") {
      obj.worktreeDir = message.worktreeDir;
    }
    if (message.prNumber !== 0) {
      obj.prNumber = Math.round(message.prNumber);
    }
    if (message.issueNumber !== 0) {
      obj.issueNumber = Math.round(message.issueNumber);
    }
    return obj;
  },

  create(base?: DeepPartial<TaskAddRequest>): TaskAddRequest {
    return TaskAddRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TaskAddRequest>): TaskAddRequest {
    const message = createBaseTaskAddRequest();
    message.dir = object.dir ?? "";
    message.body = object.body ?? "";
    message.worktreeDir = object.worktreeDir ?? "";
    message.prNumber = object.prNumber ?? 0;
    message.issueNumber = object.issueNumber ?? 0;
    return message;
  },
};

function createBaseTaskAddResponse(): TaskAddResponse {
  return { task: undefined };
}

export const TaskAddResponse: MessageFns<TaskAddResponse> = {
  encode(message: TaskAddResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.task !== undefined) {
      Task.encode(message.task, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TaskAddResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTaskAddResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.task = Task.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TaskAddResponse {
    return { task: isSet(object.task) ? Task.fromJSON(object.task) : undefined };
  },

  toJSON(message: TaskAddResponse): unknown {
    const obj: any = {};
    if (message.task !== undefined) {
      obj.task = Task.toJSON(message.task);
    }
    return obj;
  },

  create(base?: DeepPartial<TaskAddResponse>): TaskAddResponse {
    return TaskAddResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TaskAddResponse>): TaskAddResponse {
    const message = createBaseTaskAddResponse();
    message.task = (object.task !== undefined && object.task !== null) ? Task.fromPartial(object.task) : undefined;
    return message;
  },
};

function createBaseTaskUpdateRequest(): TaskUpdateRequest {
  return { dir: "", id: "", body: "" };
}

export const TaskUpdateRequest: MessageFns<TaskUpdateRequest> = {
  encode(message: TaskUpdateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dir !== "") {
      writer.uint32(10).string(message.dir);
    }
    if (message.id !== "") {
      writer.uint32(18).string(message.id);
    }
    if (message.body !== "") {
      writer.uint32(26).string(message.body);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TaskUpdateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTaskUpdateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dir = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.body = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TaskUpdateRequest {
    return {
      dir: isSet(object.dir) ? globalThis.String(object.dir) : "",
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      body: isSet(object.body) ? globalThis.String(object.body) : "",
    };
  },

  toJSON(message: TaskUpdateRequest): unknown {
    const obj: any = {};
    if (message.dir !== "") {
      obj.dir = message.dir;
    }
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.body !== "") {
      obj.body = message.body;
    }
    return obj;
  },

  create(base?: DeepPartial<TaskUpdateRequest>): TaskUpdateRequest {
    return TaskUpdateRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TaskUpdateRequest>): TaskUpdateRequest {
    const message = createBaseTaskUpdateRequest();
    message.dir = object.dir ?? "";
    message.id = object.id ?? "";
    message.body = object.body ?? "";
    return message;
  },
};

function createBaseTaskUpdateResponse(): TaskUpdateResponse {
  return { task: undefined };
}

export const TaskUpdateResponse: MessageFns<TaskUpdateResponse> = {
  encode(message: TaskUpdateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.task !== undefined) {
      Task.encode(message.task, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TaskUpdateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTaskUpdateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.task = Task.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TaskUpdateResponse {
    return { task: isSet(object.task) ? Task.fromJSON(object.task) : undefined };
  },

  toJSON(message: TaskUpdateResponse): unknown {
    const obj: any = {};
    if (message.task !== undefined) {
      obj.task = Task.toJSON(message.task);
    }
    return obj;
  },

  create(base?: DeepPartial<TaskUpdateResponse>): TaskUpdateResponse {
    return TaskUpdateResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TaskUpdateResponse>): TaskUpdateResponse {
    const message = createBaseTaskUpdateResponse();
    message.task = (object.task !== undefined && object.task !== null) ? Task.fromPartial(object.task) : undefined;
    return message;
  },
};

function createBaseTaskRemoveRequest(): TaskRemoveRequest {
  return { dir: "", id: "" };
}

export const TaskRemoveRequest: MessageFns<TaskRemoveRequest> = {
  encode(message: TaskRemoveRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dir !== "") {
      writer.uint32(10).string(message.dir);
    }
    if (message.id !== "") {
      writer.uint32(18).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TaskRemoveRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTaskRemoveRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dir = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TaskRemoveRequest {
    return {
      dir: isSet(object.dir) ? globalThis.String(object.dir) : "",
      id: isSet(object.id) ? globalThis.String(object.id) : "",
    };
  },

  toJSON(message: TaskRemoveRequest): unknown {
    const obj: any = {};
    if (message.dir !== "") {
      obj.dir = message.dir;
    }
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<TaskRemoveRequest>): TaskRemoveRequest {
    return TaskRemoveRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TaskRemoveRequest>): TaskRemoveRequest {
    const message = createBaseTaskRemoveRequest();
    message.dir = object.dir ?? "";
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseTaskRemoveResponse(): TaskRemoveResponse {
  return {};
}

export const TaskRemoveResponse: MessageFns<TaskRemoveResponse> = {
  encode(_: TaskRemoveResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TaskRemoveResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTaskRemoveResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): TaskRemoveResponse {
    return {};
  },

  toJSON(_: TaskRemoveResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<TaskRemoveResponse>): TaskRemoveResponse {
    return TaskRemoveResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<TaskRemoveResponse>): TaskRemoveResponse {
    const message = createBaseTaskRemoveResponse();
    return message;
  },
};

function createBaseCreateWorktreeWithTaskRequest(): CreateWorktreeWithTaskRequest {
  return { dir: "", id: "", worktreeDir: "", branch: "" };
}

export const CreateWorktreeWithTaskRequest: MessageFns<CreateWorktreeWithTaskRequest> = {
  encode(message: CreateWorktreeWithTaskRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dir !== "") {
      writer.uint32(10).string(message.dir);
    }
    if (message.id !== "") {
      writer.uint32(18).string(message.id);
    }
    if (message.worktreeDir !== "") {
      writer.uint32(26).string(message.worktreeDir);
    }
    if (message.branch !== "") {
      writer.uint32(34).string(message.branch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateWorktreeWithTaskRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateWorktreeWithTaskRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dir = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.worktreeDir = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.branch = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateWorktreeWithTaskRequest {
    return {
      dir: isSet(object.dir) ? globalThis.String(object.dir) : "",
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      worktreeDir: isSet(object.worktreeDir)
        ? globalThis.String(object.worktreeDir)
        : isSet(object.worktree_dir)
        ? globalThis.String(object.worktree_dir)
        : "",
      branch: isSet(object.branch) ? globalThis.String(object.branch) : "",
    };
  },

  toJSON(message: CreateWorktreeWithTaskRequest): unknown {
    const obj: any = {};
    if (message.dir !== "") {
      obj.dir = message.dir;
    }
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.worktreeDir !== "") {
      obj.worktreeDir = message.worktreeDir;
    }
    if (message.branch !== "") {
      obj.branch = message.branch;
    }
    return obj;
  },

  create(base?: DeepPartial<CreateWorktreeWithTaskRequest>): CreateWorktreeWithTaskRequest {
    return CreateWorktreeWithTaskRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateWorktreeWithTaskRequest>): CreateWorktreeWithTaskRequest {
    const message = createBaseCreateWorktreeWithTaskRequest();
    message.dir = object.dir ?? "";
    message.id = object.id ?? "";
    message.worktreeDir = object.worktreeDir ?? "";
    message.branch = object.branch ?? "";
    return message;
  },
};

function createBaseCreateWorktreeWithTaskResponse(): CreateWorktreeWithTaskResponse {
  return { task: undefined, worktree: undefined, dir: "" };
}

export const CreateWorktreeWithTaskResponse: MessageFns<CreateWorktreeWithTaskResponse> = {
  encode(message: CreateWorktreeWithTaskResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.task !== undefined) {
      Task.encode(message.task, writer.uint32(10).fork()).join();
    }
    if (message.worktree !== undefined) {
      WorktreeEntry.encode(message.worktree, writer.uint32(18).fork()).join();
    }
    if (message.dir !== "") {
      writer.uint32(26).string(message.dir);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateWorktreeWithTaskResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateWorktreeWithTaskResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.task = Task.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.worktree = WorktreeEntry.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.dir = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateWorktreeWithTaskResponse {
    return {
      task: isSet(object.task) ? Task.fromJSON(object.task) : undefined,
      worktree: isSet(object.worktree) ? WorktreeEntry.fromJSON(object.worktree) : undefined,
      dir: isSet(object.dir) ? globalThis.String(object.dir) : "",
    };
  },

  toJSON(message: CreateWorktreeWithTaskResponse): unknown {
    const obj: any = {};
    if (message.task !== undefined) {
      obj.task = Task.toJSON(message.task);
    }
    if (message.worktree !== undefined) {
      obj.worktree = WorktreeEntry.toJSON(message.worktree);
    }
    if (message.dir !== "") {
      obj.dir = message.dir;
    }
    return obj;
  },

  create(base?: DeepPartial<CreateWorktreeWithTaskResponse>): CreateWorktreeWithTaskResponse {
    return CreateWorktreeWithTaskResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateWorktreeWithTaskResponse>): CreateWorktreeWithTaskResponse {
    const message = createBaseCreateWorktreeWithTaskResponse();
    message.task = (object.task !== undefined && object.task !== null) ? Task.fromPartial(object.task) : undefined;
    message.worktree = (object.worktree !== undefined && object.worktree !== null)
      ? WorktreeEntry.fromPartial(object.worktree)
      : undefined;
    message.dir = object.dir ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
