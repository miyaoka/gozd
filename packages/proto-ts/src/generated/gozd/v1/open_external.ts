// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: gozd/v1/open_external.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "gozd.v1";

/**
 * 外部ブラウザ / 外部アプリで URL を開く。Swift 側で `NSWorkspace.shared.open(URL)`。
 *
 * 用途: xterm.js のリンク Shift+クリックで OS ブラウザに飛ばす等。
 */
export interface OpenExternalRequest {
  url: string;
}

export interface OpenExternalResponse {
}

function createBaseOpenExternalRequest(): OpenExternalRequest {
  return { url: "" };
}

export const OpenExternalRequest: MessageFns<OpenExternalRequest> = {
  encode(message: OpenExternalRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.url !== "") {
      writer.uint32(10).string(message.url);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OpenExternalRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOpenExternalRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.url = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OpenExternalRequest {
    return { url: isSet(object.url) ? globalThis.String(object.url) : "" };
  },

  toJSON(message: OpenExternalRequest): unknown {
    const obj: any = {};
    if (message.url !== "") {
      obj.url = message.url;
    }
    return obj;
  },

  create(base?: DeepPartial<OpenExternalRequest>): OpenExternalRequest {
    return OpenExternalRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OpenExternalRequest>): OpenExternalRequest {
    const message = createBaseOpenExternalRequest();
    message.url = object.url ?? "";
    return message;
  },
};

function createBaseOpenExternalResponse(): OpenExternalResponse {
  return {};
}

export const OpenExternalResponse: MessageFns<OpenExternalResponse> = {
  encode(_: OpenExternalResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OpenExternalResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOpenExternalResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): OpenExternalResponse {
    return {};
  },

  toJSON(_: OpenExternalResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<OpenExternalResponse>): OpenExternalResponse {
    return OpenExternalResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<OpenExternalResponse>): OpenExternalResponse {
    const message = createBaseOpenExternalResponse();
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
