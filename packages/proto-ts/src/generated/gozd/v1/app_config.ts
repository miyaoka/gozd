// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: gozd/v1/app_config.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "gozd.v1";

/**
 * ユーザー設定。`~/.config/gozd/config.json` に永続化される。
 *
 * 設計判断:
 *
 * 1. **proto SSOT で型付き定義**。旧実装は `Record<string, unknown>` + dot-key
 *    だったが、新実装ではセクションをネスト messages にして型を持たせる。
 *    新しい設定を追加するときは proto に field を追加する（buf generate 必須）。
 *
 * 2. **永続化形式は proto JSON mapping**。`AppStateStore` と同じ流儀で、
 *    SwiftProtobuf の `jsonString()` でファイルへ書き出す。
 */
export interface AppConfig {
  terminal: TerminalConfig | undefined;
  preview: PreviewConfig | undefined;
  voicevox: VoicevoxConfig | undefined;
}

export interface TerminalConfig {
  /** 空文字列はデフォルトテーマ（renderer 側 fallback） */
  theme: string;
  /** 空文字列なら xterm デフォルト */
  fontFamily: string;
  /** 0 なら xterm デフォルト */
  fontSize: number;
}

export interface PreviewConfig {
  fontFamily: string;
  fontSize: number;
}

export interface VoicevoxConfig {
  enabled: boolean;
  speedScale: number;
  volumeScale: number;
  /** 0 ならデフォルト (renderer 側 fallback) */
  speakerId: number;
}

export interface LoadAppConfigRequest {
}

export interface LoadAppConfigResponse {
  config: AppConfig | undefined;
}

export interface SaveAppConfigRequest {
  config: AppConfig | undefined;
}

export interface SaveAppConfigResponse {
}

function createBaseAppConfig(): AppConfig {
  return { terminal: undefined, preview: undefined, voicevox: undefined };
}

export const AppConfig: MessageFns<AppConfig> = {
  encode(message: AppConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.terminal !== undefined) {
      TerminalConfig.encode(message.terminal, writer.uint32(10).fork()).join();
    }
    if (message.preview !== undefined) {
      PreviewConfig.encode(message.preview, writer.uint32(18).fork()).join();
    }
    if (message.voicevox !== undefined) {
      VoicevoxConfig.encode(message.voicevox, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.terminal = TerminalConfig.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.preview = PreviewConfig.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.voicevox = VoicevoxConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppConfig {
    return {
      terminal: isSet(object.terminal) ? TerminalConfig.fromJSON(object.terminal) : undefined,
      preview: isSet(object.preview) ? PreviewConfig.fromJSON(object.preview) : undefined,
      voicevox: isSet(object.voicevox) ? VoicevoxConfig.fromJSON(object.voicevox) : undefined,
    };
  },

  toJSON(message: AppConfig): unknown {
    const obj: any = {};
    if (message.terminal !== undefined) {
      obj.terminal = TerminalConfig.toJSON(message.terminal);
    }
    if (message.preview !== undefined) {
      obj.preview = PreviewConfig.toJSON(message.preview);
    }
    if (message.voicevox !== undefined) {
      obj.voicevox = VoicevoxConfig.toJSON(message.voicevox);
    }
    return obj;
  },

  create(base?: DeepPartial<AppConfig>): AppConfig {
    return AppConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AppConfig>): AppConfig {
    const message = createBaseAppConfig();
    message.terminal = (object.terminal !== undefined && object.terminal !== null)
      ? TerminalConfig.fromPartial(object.terminal)
      : undefined;
    message.preview = (object.preview !== undefined && object.preview !== null)
      ? PreviewConfig.fromPartial(object.preview)
      : undefined;
    message.voicevox = (object.voicevox !== undefined && object.voicevox !== null)
      ? VoicevoxConfig.fromPartial(object.voicevox)
      : undefined;
    return message;
  },
};

function createBaseTerminalConfig(): TerminalConfig {
  return { theme: "", fontFamily: "", fontSize: 0 };
}

export const TerminalConfig: MessageFns<TerminalConfig> = {
  encode(message: TerminalConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.theme !== "") {
      writer.uint32(10).string(message.theme);
    }
    if (message.fontFamily !== "") {
      writer.uint32(18).string(message.fontFamily);
    }
    if (message.fontSize !== 0) {
      writer.uint32(24).uint32(message.fontSize);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TerminalConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTerminalConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.theme = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fontFamily = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.fontSize = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TerminalConfig {
    return {
      theme: isSet(object.theme) ? globalThis.String(object.theme) : "",
      fontFamily: isSet(object.fontFamily)
        ? globalThis.String(object.fontFamily)
        : isSet(object.font_family)
        ? globalThis.String(object.font_family)
        : "",
      fontSize: isSet(object.fontSize)
        ? globalThis.Number(object.fontSize)
        : isSet(object.font_size)
        ? globalThis.Number(object.font_size)
        : 0,
    };
  },

  toJSON(message: TerminalConfig): unknown {
    const obj: any = {};
    if (message.theme !== "") {
      obj.theme = message.theme;
    }
    if (message.fontFamily !== "") {
      obj.fontFamily = message.fontFamily;
    }
    if (message.fontSize !== 0) {
      obj.fontSize = Math.round(message.fontSize);
    }
    return obj;
  },

  create(base?: DeepPartial<TerminalConfig>): TerminalConfig {
    return TerminalConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TerminalConfig>): TerminalConfig {
    const message = createBaseTerminalConfig();
    message.theme = object.theme ?? "";
    message.fontFamily = object.fontFamily ?? "";
    message.fontSize = object.fontSize ?? 0;
    return message;
  },
};

function createBasePreviewConfig(): PreviewConfig {
  return { fontFamily: "", fontSize: 0 };
}

export const PreviewConfig: MessageFns<PreviewConfig> = {
  encode(message: PreviewConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fontFamily !== "") {
      writer.uint32(10).string(message.fontFamily);
    }
    if (message.fontSize !== 0) {
      writer.uint32(16).uint32(message.fontSize);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PreviewConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePreviewConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.fontFamily = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.fontSize = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PreviewConfig {
    return {
      fontFamily: isSet(object.fontFamily)
        ? globalThis.String(object.fontFamily)
        : isSet(object.font_family)
        ? globalThis.String(object.font_family)
        : "",
      fontSize: isSet(object.fontSize)
        ? globalThis.Number(object.fontSize)
        : isSet(object.font_size)
        ? globalThis.Number(object.font_size)
        : 0,
    };
  },

  toJSON(message: PreviewConfig): unknown {
    const obj: any = {};
    if (message.fontFamily !== "") {
      obj.fontFamily = message.fontFamily;
    }
    if (message.fontSize !== 0) {
      obj.fontSize = Math.round(message.fontSize);
    }
    return obj;
  },

  create(base?: DeepPartial<PreviewConfig>): PreviewConfig {
    return PreviewConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PreviewConfig>): PreviewConfig {
    const message = createBasePreviewConfig();
    message.fontFamily = object.fontFamily ?? "";
    message.fontSize = object.fontSize ?? 0;
    return message;
  },
};

function createBaseVoicevoxConfig(): VoicevoxConfig {
  return { enabled: false, speedScale: 0, volumeScale: 0, speakerId: 0 };
}

export const VoicevoxConfig: MessageFns<VoicevoxConfig> = {
  encode(message: VoicevoxConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.speedScale !== 0) {
      writer.uint32(17).double(message.speedScale);
    }
    if (message.volumeScale !== 0) {
      writer.uint32(25).double(message.volumeScale);
    }
    if (message.speakerId !== 0) {
      writer.uint32(32).uint32(message.speakerId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VoicevoxConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVoicevoxConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.speedScale = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.volumeScale = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.speakerId = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VoicevoxConfig {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      speedScale: isSet(object.speedScale)
        ? globalThis.Number(object.speedScale)
        : isSet(object.speed_scale)
        ? globalThis.Number(object.speed_scale)
        : 0,
      volumeScale: isSet(object.volumeScale)
        ? globalThis.Number(object.volumeScale)
        : isSet(object.volume_scale)
        ? globalThis.Number(object.volume_scale)
        : 0,
      speakerId: isSet(object.speakerId)
        ? globalThis.Number(object.speakerId)
        : isSet(object.speaker_id)
        ? globalThis.Number(object.speaker_id)
        : 0,
    };
  },

  toJSON(message: VoicevoxConfig): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.speedScale !== 0) {
      obj.speedScale = message.speedScale;
    }
    if (message.volumeScale !== 0) {
      obj.volumeScale = message.volumeScale;
    }
    if (message.speakerId !== 0) {
      obj.speakerId = Math.round(message.speakerId);
    }
    return obj;
  },

  create(base?: DeepPartial<VoicevoxConfig>): VoicevoxConfig {
    return VoicevoxConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<VoicevoxConfig>): VoicevoxConfig {
    const message = createBaseVoicevoxConfig();
    message.enabled = object.enabled ?? false;
    message.speedScale = object.speedScale ?? 0;
    message.volumeScale = object.volumeScale ?? 0;
    message.speakerId = object.speakerId ?? 0;
    return message;
  },
};

function createBaseLoadAppConfigRequest(): LoadAppConfigRequest {
  return {};
}

export const LoadAppConfigRequest: MessageFns<LoadAppConfigRequest> = {
  encode(_: LoadAppConfigRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoadAppConfigRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoadAppConfigRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): LoadAppConfigRequest {
    return {};
  },

  toJSON(_: LoadAppConfigRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<LoadAppConfigRequest>): LoadAppConfigRequest {
    return LoadAppConfigRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<LoadAppConfigRequest>): LoadAppConfigRequest {
    const message = createBaseLoadAppConfigRequest();
    return message;
  },
};

function createBaseLoadAppConfigResponse(): LoadAppConfigResponse {
  return { config: undefined };
}

export const LoadAppConfigResponse: MessageFns<LoadAppConfigResponse> = {
  encode(message: LoadAppConfigResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.config !== undefined) {
      AppConfig.encode(message.config, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoadAppConfigResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoadAppConfigResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.config = AppConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LoadAppConfigResponse {
    return { config: isSet(object.config) ? AppConfig.fromJSON(object.config) : undefined };
  },

  toJSON(message: LoadAppConfigResponse): unknown {
    const obj: any = {};
    if (message.config !== undefined) {
      obj.config = AppConfig.toJSON(message.config);
    }
    return obj;
  },

  create(base?: DeepPartial<LoadAppConfigResponse>): LoadAppConfigResponse {
    return LoadAppConfigResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LoadAppConfigResponse>): LoadAppConfigResponse {
    const message = createBaseLoadAppConfigResponse();
    message.config = (object.config !== undefined && object.config !== null)
      ? AppConfig.fromPartial(object.config)
      : undefined;
    return message;
  },
};

function createBaseSaveAppConfigRequest(): SaveAppConfigRequest {
  return { config: undefined };
}

export const SaveAppConfigRequest: MessageFns<SaveAppConfigRequest> = {
  encode(message: SaveAppConfigRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.config !== undefined) {
      AppConfig.encode(message.config, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SaveAppConfigRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSaveAppConfigRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.config = AppConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SaveAppConfigRequest {
    return { config: isSet(object.config) ? AppConfig.fromJSON(object.config) : undefined };
  },

  toJSON(message: SaveAppConfigRequest): unknown {
    const obj: any = {};
    if (message.config !== undefined) {
      obj.config = AppConfig.toJSON(message.config);
    }
    return obj;
  },

  create(base?: DeepPartial<SaveAppConfigRequest>): SaveAppConfigRequest {
    return SaveAppConfigRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SaveAppConfigRequest>): SaveAppConfigRequest {
    const message = createBaseSaveAppConfigRequest();
    message.config = (object.config !== undefined && object.config !== null)
      ? AppConfig.fromPartial(object.config)
      : undefined;
    return message;
  },
};

function createBaseSaveAppConfigResponse(): SaveAppConfigResponse {
  return {};
}

export const SaveAppConfigResponse: MessageFns<SaveAppConfigResponse> = {
  encode(_: SaveAppConfigResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SaveAppConfigResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSaveAppConfigResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): SaveAppConfigResponse {
    return {};
  },

  toJSON(_: SaveAppConfigResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<SaveAppConfigResponse>): SaveAppConfigResponse {
    return SaveAppConfigResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<SaveAppConfigResponse>): SaveAppConfigResponse {
    const message = createBaseSaveAppConfigResponse();
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
