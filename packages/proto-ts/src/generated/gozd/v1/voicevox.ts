// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: gozd/v1/voicevox.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "gozd.v1";

export interface VoicevoxLaunchRequest {
}

export interface VoicevoxLaunchResponse {
  ok: boolean;
}

export interface VoicevoxCheckEngineRequest {
}

export interface VoicevoxCheckEngineResponse {
  ok: boolean;
}

export interface VoicevoxSpeakRequest {
  text: string;
  speedScale: number;
  volumeScale: number;
  speakerId: number;
}

export interface VoicevoxSpeakResponse {
  /** 合成成功時の wav バイト列。失敗時は空。再生は呼び出し側（renderer）の責務。 */
  wav: Uint8Array;
}

function createBaseVoicevoxLaunchRequest(): VoicevoxLaunchRequest {
  return {};
}

export const VoicevoxLaunchRequest: MessageFns<VoicevoxLaunchRequest> = {
  encode(_: VoicevoxLaunchRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VoicevoxLaunchRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVoicevoxLaunchRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): VoicevoxLaunchRequest {
    return {};
  },

  toJSON(_: VoicevoxLaunchRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<VoicevoxLaunchRequest>): VoicevoxLaunchRequest {
    return VoicevoxLaunchRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<VoicevoxLaunchRequest>): VoicevoxLaunchRequest {
    const message = createBaseVoicevoxLaunchRequest();
    return message;
  },
};

function createBaseVoicevoxLaunchResponse(): VoicevoxLaunchResponse {
  return { ok: false };
}

export const VoicevoxLaunchResponse: MessageFns<VoicevoxLaunchResponse> = {
  encode(message: VoicevoxLaunchResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VoicevoxLaunchResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVoicevoxLaunchResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VoicevoxLaunchResponse {
    return { ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false };
  },

  toJSON(message: VoicevoxLaunchResponse): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    return obj;
  },

  create(base?: DeepPartial<VoicevoxLaunchResponse>): VoicevoxLaunchResponse {
    return VoicevoxLaunchResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<VoicevoxLaunchResponse>): VoicevoxLaunchResponse {
    const message = createBaseVoicevoxLaunchResponse();
    message.ok = object.ok ?? false;
    return message;
  },
};

function createBaseVoicevoxCheckEngineRequest(): VoicevoxCheckEngineRequest {
  return {};
}

export const VoicevoxCheckEngineRequest: MessageFns<VoicevoxCheckEngineRequest> = {
  encode(_: VoicevoxCheckEngineRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VoicevoxCheckEngineRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVoicevoxCheckEngineRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): VoicevoxCheckEngineRequest {
    return {};
  },

  toJSON(_: VoicevoxCheckEngineRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<VoicevoxCheckEngineRequest>): VoicevoxCheckEngineRequest {
    return VoicevoxCheckEngineRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<VoicevoxCheckEngineRequest>): VoicevoxCheckEngineRequest {
    const message = createBaseVoicevoxCheckEngineRequest();
    return message;
  },
};

function createBaseVoicevoxCheckEngineResponse(): VoicevoxCheckEngineResponse {
  return { ok: false };
}

export const VoicevoxCheckEngineResponse: MessageFns<VoicevoxCheckEngineResponse> = {
  encode(message: VoicevoxCheckEngineResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VoicevoxCheckEngineResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVoicevoxCheckEngineResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VoicevoxCheckEngineResponse {
    return { ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false };
  },

  toJSON(message: VoicevoxCheckEngineResponse): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    return obj;
  },

  create(base?: DeepPartial<VoicevoxCheckEngineResponse>): VoicevoxCheckEngineResponse {
    return VoicevoxCheckEngineResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<VoicevoxCheckEngineResponse>): VoicevoxCheckEngineResponse {
    const message = createBaseVoicevoxCheckEngineResponse();
    message.ok = object.ok ?? false;
    return message;
  },
};

function createBaseVoicevoxSpeakRequest(): VoicevoxSpeakRequest {
  return { text: "", speedScale: 0, volumeScale: 0, speakerId: 0 };
}

export const VoicevoxSpeakRequest: MessageFns<VoicevoxSpeakRequest> = {
  encode(message: VoicevoxSpeakRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.speedScale !== 0) {
      writer.uint32(17).double(message.speedScale);
    }
    if (message.volumeScale !== 0) {
      writer.uint32(25).double(message.volumeScale);
    }
    if (message.speakerId !== 0) {
      writer.uint32(32).uint32(message.speakerId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VoicevoxSpeakRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVoicevoxSpeakRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.speedScale = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.volumeScale = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.speakerId = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VoicevoxSpeakRequest {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      speedScale: isSet(object.speedScale)
        ? globalThis.Number(object.speedScale)
        : isSet(object.speed_scale)
        ? globalThis.Number(object.speed_scale)
        : 0,
      volumeScale: isSet(object.volumeScale)
        ? globalThis.Number(object.volumeScale)
        : isSet(object.volume_scale)
        ? globalThis.Number(object.volume_scale)
        : 0,
      speakerId: isSet(object.speakerId)
        ? globalThis.Number(object.speakerId)
        : isSet(object.speaker_id)
        ? globalThis.Number(object.speaker_id)
        : 0,
    };
  },

  toJSON(message: VoicevoxSpeakRequest): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.speedScale !== 0) {
      obj.speedScale = message.speedScale;
    }
    if (message.volumeScale !== 0) {
      obj.volumeScale = message.volumeScale;
    }
    if (message.speakerId !== 0) {
      obj.speakerId = Math.round(message.speakerId);
    }
    return obj;
  },

  create(base?: DeepPartial<VoicevoxSpeakRequest>): VoicevoxSpeakRequest {
    return VoicevoxSpeakRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<VoicevoxSpeakRequest>): VoicevoxSpeakRequest {
    const message = createBaseVoicevoxSpeakRequest();
    message.text = object.text ?? "";
    message.speedScale = object.speedScale ?? 0;
    message.volumeScale = object.volumeScale ?? 0;
    message.speakerId = object.speakerId ?? 0;
    return message;
  },
};

function createBaseVoicevoxSpeakResponse(): VoicevoxSpeakResponse {
  return { wav: new Uint8Array(0) };
}

export const VoicevoxSpeakResponse: MessageFns<VoicevoxSpeakResponse> = {
  encode(message: VoicevoxSpeakResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.wav.length !== 0) {
      writer.uint32(10).bytes(message.wav);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VoicevoxSpeakResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVoicevoxSpeakResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.wav = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VoicevoxSpeakResponse {
    return { wav: isSet(object.wav) ? bytesFromBase64(object.wav) : new Uint8Array(0) };
  },

  toJSON(message: VoicevoxSpeakResponse): unknown {
    const obj: any = {};
    if (message.wav.length !== 0) {
      obj.wav = base64FromBytes(message.wav);
    }
    return obj;
  },

  create(base?: DeepPartial<VoicevoxSpeakResponse>): VoicevoxSpeakResponse {
    return VoicevoxSpeakResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<VoicevoxSpeakResponse>): VoicevoxSpeakResponse {
    const message = createBaseVoicevoxSpeakResponse();
    message.wav = object.wav ?? new Uint8Array(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
