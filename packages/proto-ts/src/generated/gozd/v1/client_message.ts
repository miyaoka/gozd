// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: gozd/v1/client_message.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "gozd.v1";

/**
 * CLI / nc から SocketServer へ送られるメッセージ。
 * NDJSON 1 行 = 1 ClientMessage（proto3 JSON mapping）。
 *
 * proto3 oneof は JSON で `{"hook":{...}}` / `{"open":{...}}` の形式になり、
 * nc 直送コマンドにそのまま埋め込める：
 *
 *   echo '{"hook":{"event":"session-start","ptyId":'"$GOZD_PTY_ID"'}}' | nc -w 1 -U "$GOZD_SOCKET_PATH"
 */
export interface ClientMessage {
  hook?: HookMessage | undefined;
  open?: OpenMessage | undefined;
}

/**
 * Claude Code の hook イベント通知。
 * 軽量な nc 直送経路では event / pty_id のみ。CLI 経由の rich 経路では
 * 残りの payload フィールドが埋まる。
 */
export interface HookMessage {
  /**
   * "session-start" / "session-end" / "running" / "done" / "needs-input" /
   * "tool-done" / "tool-failure" / "stop-failure"
   */
  event: string;
  ptyId: number;
  /** CLI 経由のみ */
  lastAssistantMessage: string;
  toolName: string;
  /** Claude Code の tool_input をそのまま JSON 文字列として保持 */
  toolInput: string;
  isInterrupt: boolean;
  /** session-start / session-end のみ。Claude Code の resume 起動に必要。 */
  sessionId: string;
  transcriptPath: string;
  /** session-start のみ。"startup" / "resume" / "clear" / "compact" 等 */
  source: string;
}

/** `gozd open <path>` から送られるプロジェクトを開けの指示。 */
export interface OpenMessage {
  targetPath: string;
}

function createBaseClientMessage(): ClientMessage {
  return { hook: undefined, open: undefined };
}

export const ClientMessage: MessageFns<ClientMessage> = {
  encode(message: ClientMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hook !== undefined) {
      HookMessage.encode(message.hook, writer.uint32(10).fork()).join();
    }
    if (message.open !== undefined) {
      OpenMessage.encode(message.open, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClientMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClientMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hook = HookMessage.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.open = OpenMessage.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClientMessage {
    return {
      hook: isSet(object.hook) ? HookMessage.fromJSON(object.hook) : undefined,
      open: isSet(object.open) ? OpenMessage.fromJSON(object.open) : undefined,
    };
  },

  toJSON(message: ClientMessage): unknown {
    const obj: any = {};
    if (message.hook !== undefined) {
      obj.hook = HookMessage.toJSON(message.hook);
    }
    if (message.open !== undefined) {
      obj.open = OpenMessage.toJSON(message.open);
    }
    return obj;
  },

  create(base?: DeepPartial<ClientMessage>): ClientMessage {
    return ClientMessage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ClientMessage>): ClientMessage {
    const message = createBaseClientMessage();
    message.hook = (object.hook !== undefined && object.hook !== null)
      ? HookMessage.fromPartial(object.hook)
      : undefined;
    message.open = (object.open !== undefined && object.open !== null)
      ? OpenMessage.fromPartial(object.open)
      : undefined;
    return message;
  },
};

function createBaseHookMessage(): HookMessage {
  return {
    event: "",
    ptyId: 0,
    lastAssistantMessage: "",
    toolName: "",
    toolInput: "",
    isInterrupt: false,
    sessionId: "",
    transcriptPath: "",
    source: "",
  };
}

export const HookMessage: MessageFns<HookMessage> = {
  encode(message: HookMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.event !== "") {
      writer.uint32(10).string(message.event);
    }
    if (message.ptyId !== 0) {
      writer.uint32(16).uint32(message.ptyId);
    }
    if (message.lastAssistantMessage !== "") {
      writer.uint32(26).string(message.lastAssistantMessage);
    }
    if (message.toolName !== "") {
      writer.uint32(34).string(message.toolName);
    }
    if (message.toolInput !== "") {
      writer.uint32(42).string(message.toolInput);
    }
    if (message.isInterrupt !== false) {
      writer.uint32(48).bool(message.isInterrupt);
    }
    if (message.sessionId !== "") {
      writer.uint32(58).string(message.sessionId);
    }
    if (message.transcriptPath !== "") {
      writer.uint32(66).string(message.transcriptPath);
    }
    if (message.source !== "") {
      writer.uint32(74).string(message.source);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HookMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHookMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.event = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.ptyId = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.lastAssistantMessage = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.toolName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.toolInput = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.isInterrupt = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.transcriptPath = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.source = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HookMessage {
    return {
      event: isSet(object.event) ? globalThis.String(object.event) : "",
      ptyId: isSet(object.ptyId)
        ? globalThis.Number(object.ptyId)
        : isSet(object.pty_id)
        ? globalThis.Number(object.pty_id)
        : 0,
      lastAssistantMessage: isSet(object.lastAssistantMessage)
        ? globalThis.String(object.lastAssistantMessage)
        : isSet(object.last_assistant_message)
        ? globalThis.String(object.last_assistant_message)
        : "",
      toolName: isSet(object.toolName)
        ? globalThis.String(object.toolName)
        : isSet(object.tool_name)
        ? globalThis.String(object.tool_name)
        : "",
      toolInput: isSet(object.toolInput)
        ? globalThis.String(object.toolInput)
        : isSet(object.tool_input)
        ? globalThis.String(object.tool_input)
        : "",
      isInterrupt: isSet(object.isInterrupt)
        ? globalThis.Boolean(object.isInterrupt)
        : isSet(object.is_interrupt)
        ? globalThis.Boolean(object.is_interrupt)
        : false,
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      transcriptPath: isSet(object.transcriptPath)
        ? globalThis.String(object.transcriptPath)
        : isSet(object.transcript_path)
        ? globalThis.String(object.transcript_path)
        : "",
      source: isSet(object.source) ? globalThis.String(object.source) : "",
    };
  },

  toJSON(message: HookMessage): unknown {
    const obj: any = {};
    if (message.event !== "") {
      obj.event = message.event;
    }
    if (message.ptyId !== 0) {
      obj.ptyId = Math.round(message.ptyId);
    }
    if (message.lastAssistantMessage !== "") {
      obj.lastAssistantMessage = message.lastAssistantMessage;
    }
    if (message.toolName !== "") {
      obj.toolName = message.toolName;
    }
    if (message.toolInput !== "") {
      obj.toolInput = message.toolInput;
    }
    if (message.isInterrupt !== false) {
      obj.isInterrupt = message.isInterrupt;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.transcriptPath !== "") {
      obj.transcriptPath = message.transcriptPath;
    }
    if (message.source !== "") {
      obj.source = message.source;
    }
    return obj;
  },

  create(base?: DeepPartial<HookMessage>): HookMessage {
    return HookMessage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HookMessage>): HookMessage {
    const message = createBaseHookMessage();
    message.event = object.event ?? "";
    message.ptyId = object.ptyId ?? 0;
    message.lastAssistantMessage = object.lastAssistantMessage ?? "";
    message.toolName = object.toolName ?? "";
    message.toolInput = object.toolInput ?? "";
    message.isInterrupt = object.isInterrupt ?? false;
    message.sessionId = object.sessionId ?? "";
    message.transcriptPath = object.transcriptPath ?? "";
    message.source = object.source ?? "";
    return message;
  },
};

function createBaseOpenMessage(): OpenMessage {
  return { targetPath: "" };
}

export const OpenMessage: MessageFns<OpenMessage> = {
  encode(message: OpenMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.targetPath !== "") {
      writer.uint32(10).string(message.targetPath);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OpenMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOpenMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.targetPath = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OpenMessage {
    return {
      targetPath: isSet(object.targetPath)
        ? globalThis.String(object.targetPath)
        : isSet(object.target_path)
        ? globalThis.String(object.target_path)
        : "",
    };
  },

  toJSON(message: OpenMessage): unknown {
    const obj: any = {};
    if (message.targetPath !== "") {
      obj.targetPath = message.targetPath;
    }
    return obj;
  },

  create(base?: DeepPartial<OpenMessage>): OpenMessage {
    return OpenMessage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OpenMessage>): OpenMessage {
    const message = createBaseOpenMessage();
    message.targetPath = object.targetPath ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
