// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: gozd/v1/common.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "gozd.v1";

export interface WorktreeEntry {
  path: string;
  head: string;
  branch: string;
  isMain: boolean;
  /** ファイル相対パス → porcelain v1 XY コード */
  gitStatuses: { [key: string]: string };
  tasks: Task[];
}

export interface WorktreeEntry_GitStatusesEntry {
  key: string;
  value: string;
}

export interface Task {
  /** UUID。Claude session とは独立した task 固有の identity。 */
  id: string;
  body: string;
  worktreeDir: string;
  prNumber: number;
  issueNumber: number;
  /** ISO 8601 */
  createdAt: string;
  /**
   * 最後に attach された Claude session の ID。空文字は session 未起動 / 終了済み。
   * SessionEnd では消さず保持し、サイドバークリック時の `claude --resume` 起点に使う。
   */
  sessionId: string;
}

/** ディレクトリエントリ。fs/readDir / filer 用。 */
export interface FileEntry {
  name: string;
  /** "file" / "directory" / "symlink" */
  type: string;
}

export interface FileReadResult {
  content: string;
  isBinary: boolean;
  isDirectory: boolean;
  notFound: boolean;
}

export interface GitFileChange {
  oldFilePath: string;
  newFilePath: string;
  /** "A" / "M" / "D" / "R" / "U" */
  type: string;
}

export interface GitCommit {
  hash: string;
  shortHash: string;
  parents: string[];
  author: string;
  /** Unix timestamp（秒） */
  date: number;
  message: string;
  body: string;
  refs: string[];
}

export interface GitPullRequest {
  number: number;
  title: string;
  url: string;
  state: string;
  author: string;
  headRef: string;
  baseRef: string;
  isDraft: boolean;
  assignees: string[];
  reviewers: string[];
  /** ISO 8601 */
  updatedAt: string;
  authorAvatarUrl: string;
}

export interface GitIssue {
  number: number;
  title: string;
  url: string;
  state: string;
  author: string;
  labels: string[];
  assignees: string[];
  updatedAt: string;
  authorAvatarUrl: string;
}

export interface OpenTargetSelection {
  /** "file" / "dir" */
  kind: string;
  relPath: string;
  /** 1-based */
  lineNumber: number;
}

export interface ProjectConfig {
  /** worktree 作成時にメインリポジトリからシンボリックリンクする相対パス一覧 */
  worktreeSymlinks: string[];
}

function createBaseWorktreeEntry(): WorktreeEntry {
  return { path: "", head: "", branch: "", isMain: false, gitStatuses: {}, tasks: [] };
}

export const WorktreeEntry: MessageFns<WorktreeEntry> = {
  encode(message: WorktreeEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.head !== "") {
      writer.uint32(18).string(message.head);
    }
    if (message.branch !== "") {
      writer.uint32(26).string(message.branch);
    }
    if (message.isMain !== false) {
      writer.uint32(32).bool(message.isMain);
    }
    globalThis.Object.entries(message.gitStatuses).forEach(([key, value]: [string, string]) => {
      WorktreeEntry_GitStatusesEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    for (const v of message.tasks) {
      Task.encode(v!, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorktreeEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorktreeEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.head = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.branch = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.isMain = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = WorktreeEntry_GitStatusesEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.gitStatuses[entry5.key] = entry5.value;
          }
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.tasks.push(Task.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorktreeEntry {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      head: isSet(object.head) ? globalThis.String(object.head) : "",
      branch: isSet(object.branch) ? globalThis.String(object.branch) : "",
      isMain: isSet(object.isMain)
        ? globalThis.Boolean(object.isMain)
        : isSet(object.is_main)
        ? globalThis.Boolean(object.is_main)
        : false,
      gitStatuses: isObject(object.gitStatuses)
        ? (globalThis.Object.entries(object.gitStatuses) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : isObject(object.git_statuses)
        ? (globalThis.Object.entries(object.git_statuses) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      tasks: globalThis.Array.isArray(object?.tasks) ? object.tasks.map((e: any) => Task.fromJSON(e)) : [],
    };
  },

  toJSON(message: WorktreeEntry): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.head !== "") {
      obj.head = message.head;
    }
    if (message.branch !== "") {
      obj.branch = message.branch;
    }
    if (message.isMain !== false) {
      obj.isMain = message.isMain;
    }
    if (message.gitStatuses) {
      const entries = globalThis.Object.entries(message.gitStatuses) as [string, string][];
      if (entries.length > 0) {
        obj.gitStatuses = {};
        entries.forEach(([k, v]) => {
          obj.gitStatuses[k] = v;
        });
      }
    }
    if (message.tasks?.length) {
      obj.tasks = message.tasks.map((e) => Task.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<WorktreeEntry>): WorktreeEntry {
    return WorktreeEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WorktreeEntry>): WorktreeEntry {
    const message = createBaseWorktreeEntry();
    message.path = object.path ?? "";
    message.head = object.head ?? "";
    message.branch = object.branch ?? "";
    message.isMain = object.isMain ?? false;
    message.gitStatuses = (globalThis.Object.entries(object.gitStatuses ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.tasks = object.tasks?.map((e) => Task.fromPartial(e)) || [];
    return message;
  },
};

function createBaseWorktreeEntry_GitStatusesEntry(): WorktreeEntry_GitStatusesEntry {
  return { key: "", value: "" };
}

export const WorktreeEntry_GitStatusesEntry: MessageFns<WorktreeEntry_GitStatusesEntry> = {
  encode(message: WorktreeEntry_GitStatusesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorktreeEntry_GitStatusesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorktreeEntry_GitStatusesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorktreeEntry_GitStatusesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: WorktreeEntry_GitStatusesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<WorktreeEntry_GitStatusesEntry>): WorktreeEntry_GitStatusesEntry {
    return WorktreeEntry_GitStatusesEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WorktreeEntry_GitStatusesEntry>): WorktreeEntry_GitStatusesEntry {
    const message = createBaseWorktreeEntry_GitStatusesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseTask(): Task {
  return { id: "", body: "", worktreeDir: "", prNumber: 0, issueNumber: 0, createdAt: "", sessionId: "" };
}

export const Task: MessageFns<Task> = {
  encode(message: Task, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.body !== "") {
      writer.uint32(18).string(message.body);
    }
    if (message.worktreeDir !== "") {
      writer.uint32(26).string(message.worktreeDir);
    }
    if (message.prNumber !== 0) {
      writer.uint32(32).uint32(message.prNumber);
    }
    if (message.issueNumber !== 0) {
      writer.uint32(40).uint32(message.issueNumber);
    }
    if (message.createdAt !== "") {
      writer.uint32(50).string(message.createdAt);
    }
    if (message.sessionId !== "") {
      writer.uint32(58).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Task {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTask();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.body = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.worktreeDir = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.prNumber = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.issueNumber = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Task {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      body: isSet(object.body) ? globalThis.String(object.body) : "",
      worktreeDir: isSet(object.worktreeDir)
        ? globalThis.String(object.worktreeDir)
        : isSet(object.worktree_dir)
        ? globalThis.String(object.worktree_dir)
        : "",
      prNumber: isSet(object.prNumber)
        ? globalThis.Number(object.prNumber)
        : isSet(object.pr_number)
        ? globalThis.Number(object.pr_number)
        : 0,
      issueNumber: isSet(object.issueNumber)
        ? globalThis.Number(object.issueNumber)
        : isSet(object.issue_number)
        ? globalThis.Number(object.issue_number)
        : 0,
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
    };
  },

  toJSON(message: Task): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.body !== "") {
      obj.body = message.body;
    }
    if (message.worktreeDir !== "") {
      obj.worktreeDir = message.worktreeDir;
    }
    if (message.prNumber !== 0) {
      obj.prNumber = Math.round(message.prNumber);
    }
    if (message.issueNumber !== 0) {
      obj.issueNumber = Math.round(message.issueNumber);
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create(base?: DeepPartial<Task>): Task {
    return Task.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Task>): Task {
    const message = createBaseTask();
    message.id = object.id ?? "";
    message.body = object.body ?? "";
    message.worktreeDir = object.worktreeDir ?? "";
    message.prNumber = object.prNumber ?? 0;
    message.issueNumber = object.issueNumber ?? 0;
    message.createdAt = object.createdAt ?? "";
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseFileEntry(): FileEntry {
  return { name: "", type: "" };
}

export const FileEntry: MessageFns<FileEntry> = {
  encode(message: FileEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.type !== "") {
      writer.uint32(18).string(message.type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.type = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileEntry {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
    };
  },

  toJSON(message: FileEntry): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    return obj;
  },

  create(base?: DeepPartial<FileEntry>): FileEntry {
    return FileEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FileEntry>): FileEntry {
    const message = createBaseFileEntry();
    message.name = object.name ?? "";
    message.type = object.type ?? "";
    return message;
  },
};

function createBaseFileReadResult(): FileReadResult {
  return { content: "", isBinary: false, isDirectory: false, notFound: false };
}

export const FileReadResult: MessageFns<FileReadResult> = {
  encode(message: FileReadResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.content !== "") {
      writer.uint32(10).string(message.content);
    }
    if (message.isBinary !== false) {
      writer.uint32(16).bool(message.isBinary);
    }
    if (message.isDirectory !== false) {
      writer.uint32(24).bool(message.isDirectory);
    }
    if (message.notFound !== false) {
      writer.uint32(32).bool(message.notFound);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileReadResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileReadResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.content = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isBinary = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isDirectory = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.notFound = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileReadResult {
    return {
      content: isSet(object.content) ? globalThis.String(object.content) : "",
      isBinary: isSet(object.isBinary)
        ? globalThis.Boolean(object.isBinary)
        : isSet(object.is_binary)
        ? globalThis.Boolean(object.is_binary)
        : false,
      isDirectory: isSet(object.isDirectory)
        ? globalThis.Boolean(object.isDirectory)
        : isSet(object.is_directory)
        ? globalThis.Boolean(object.is_directory)
        : false,
      notFound: isSet(object.notFound)
        ? globalThis.Boolean(object.notFound)
        : isSet(object.not_found)
        ? globalThis.Boolean(object.not_found)
        : false,
    };
  },

  toJSON(message: FileReadResult): unknown {
    const obj: any = {};
    if (message.content !== "") {
      obj.content = message.content;
    }
    if (message.isBinary !== false) {
      obj.isBinary = message.isBinary;
    }
    if (message.isDirectory !== false) {
      obj.isDirectory = message.isDirectory;
    }
    if (message.notFound !== false) {
      obj.notFound = message.notFound;
    }
    return obj;
  },

  create(base?: DeepPartial<FileReadResult>): FileReadResult {
    return FileReadResult.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FileReadResult>): FileReadResult {
    const message = createBaseFileReadResult();
    message.content = object.content ?? "";
    message.isBinary = object.isBinary ?? false;
    message.isDirectory = object.isDirectory ?? false;
    message.notFound = object.notFound ?? false;
    return message;
  },
};

function createBaseGitFileChange(): GitFileChange {
  return { oldFilePath: "", newFilePath: "", type: "" };
}

export const GitFileChange: MessageFns<GitFileChange> = {
  encode(message: GitFileChange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.oldFilePath !== "") {
      writer.uint32(10).string(message.oldFilePath);
    }
    if (message.newFilePath !== "") {
      writer.uint32(18).string(message.newFilePath);
    }
    if (message.type !== "") {
      writer.uint32(26).string(message.type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GitFileChange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGitFileChange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.oldFilePath = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.newFilePath = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.type = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GitFileChange {
    return {
      oldFilePath: isSet(object.oldFilePath)
        ? globalThis.String(object.oldFilePath)
        : isSet(object.old_file_path)
        ? globalThis.String(object.old_file_path)
        : "",
      newFilePath: isSet(object.newFilePath)
        ? globalThis.String(object.newFilePath)
        : isSet(object.new_file_path)
        ? globalThis.String(object.new_file_path)
        : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
    };
  },

  toJSON(message: GitFileChange): unknown {
    const obj: any = {};
    if (message.oldFilePath !== "") {
      obj.oldFilePath = message.oldFilePath;
    }
    if (message.newFilePath !== "") {
      obj.newFilePath = message.newFilePath;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    return obj;
  },

  create(base?: DeepPartial<GitFileChange>): GitFileChange {
    return GitFileChange.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GitFileChange>): GitFileChange {
    const message = createBaseGitFileChange();
    message.oldFilePath = object.oldFilePath ?? "";
    message.newFilePath = object.newFilePath ?? "";
    message.type = object.type ?? "";
    return message;
  },
};

function createBaseGitCommit(): GitCommit {
  return { hash: "", shortHash: "", parents: [], author: "", date: 0, message: "", body: "", refs: [] };
}

export const GitCommit: MessageFns<GitCommit> = {
  encode(message: GitCommit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hash !== "") {
      writer.uint32(10).string(message.hash);
    }
    if (message.shortHash !== "") {
      writer.uint32(18).string(message.shortHash);
    }
    for (const v of message.parents) {
      writer.uint32(26).string(v!);
    }
    if (message.author !== "") {
      writer.uint32(34).string(message.author);
    }
    if (message.date !== 0) {
      writer.uint32(40).int64(message.date);
    }
    if (message.message !== "") {
      writer.uint32(50).string(message.message);
    }
    if (message.body !== "") {
      writer.uint32(58).string(message.body);
    }
    for (const v of message.refs) {
      writer.uint32(66).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GitCommit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGitCommit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hash = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.shortHash = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.parents.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.author = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.date = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.body = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.refs.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GitCommit {
    return {
      hash: isSet(object.hash) ? globalThis.String(object.hash) : "",
      shortHash: isSet(object.shortHash)
        ? globalThis.String(object.shortHash)
        : isSet(object.short_hash)
        ? globalThis.String(object.short_hash)
        : "",
      parents: globalThis.Array.isArray(object?.parents) ? object.parents.map((e: any) => globalThis.String(e)) : [],
      author: isSet(object.author) ? globalThis.String(object.author) : "",
      date: isSet(object.date) ? globalThis.Number(object.date) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      body: isSet(object.body) ? globalThis.String(object.body) : "",
      refs: globalThis.Array.isArray(object?.refs) ? object.refs.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: GitCommit): unknown {
    const obj: any = {};
    if (message.hash !== "") {
      obj.hash = message.hash;
    }
    if (message.shortHash !== "") {
      obj.shortHash = message.shortHash;
    }
    if (message.parents?.length) {
      obj.parents = message.parents;
    }
    if (message.author !== "") {
      obj.author = message.author;
    }
    if (message.date !== 0) {
      obj.date = Math.round(message.date);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.body !== "") {
      obj.body = message.body;
    }
    if (message.refs?.length) {
      obj.refs = message.refs;
    }
    return obj;
  },

  create(base?: DeepPartial<GitCommit>): GitCommit {
    return GitCommit.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GitCommit>): GitCommit {
    const message = createBaseGitCommit();
    message.hash = object.hash ?? "";
    message.shortHash = object.shortHash ?? "";
    message.parents = object.parents?.map((e) => e) || [];
    message.author = object.author ?? "";
    message.date = object.date ?? 0;
    message.message = object.message ?? "";
    message.body = object.body ?? "";
    message.refs = object.refs?.map((e) => e) || [];
    return message;
  },
};

function createBaseGitPullRequest(): GitPullRequest {
  return {
    number: 0,
    title: "",
    url: "",
    state: "",
    author: "",
    headRef: "",
    baseRef: "",
    isDraft: false,
    assignees: [],
    reviewers: [],
    updatedAt: "",
    authorAvatarUrl: "",
  };
}

export const GitPullRequest: MessageFns<GitPullRequest> = {
  encode(message: GitPullRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.number !== 0) {
      writer.uint32(8).uint32(message.number);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.url !== "") {
      writer.uint32(26).string(message.url);
    }
    if (message.state !== "") {
      writer.uint32(34).string(message.state);
    }
    if (message.author !== "") {
      writer.uint32(42).string(message.author);
    }
    if (message.headRef !== "") {
      writer.uint32(50).string(message.headRef);
    }
    if (message.baseRef !== "") {
      writer.uint32(58).string(message.baseRef);
    }
    if (message.isDraft !== false) {
      writer.uint32(64).bool(message.isDraft);
    }
    for (const v of message.assignees) {
      writer.uint32(74).string(v!);
    }
    for (const v of message.reviewers) {
      writer.uint32(82).string(v!);
    }
    if (message.updatedAt !== "") {
      writer.uint32(90).string(message.updatedAt);
    }
    if (message.authorAvatarUrl !== "") {
      writer.uint32(98).string(message.authorAvatarUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GitPullRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGitPullRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.number = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.state = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.author = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.headRef = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.baseRef = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.isDraft = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.assignees.push(reader.string());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.reviewers.push(reader.string());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.updatedAt = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.authorAvatarUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GitPullRequest {
    return {
      number: isSet(object.number) ? globalThis.Number(object.number) : 0,
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      state: isSet(object.state) ? globalThis.String(object.state) : "",
      author: isSet(object.author) ? globalThis.String(object.author) : "",
      headRef: isSet(object.headRef)
        ? globalThis.String(object.headRef)
        : isSet(object.head_ref)
        ? globalThis.String(object.head_ref)
        : "",
      baseRef: isSet(object.baseRef)
        ? globalThis.String(object.baseRef)
        : isSet(object.base_ref)
        ? globalThis.String(object.base_ref)
        : "",
      isDraft: isSet(object.isDraft)
        ? globalThis.Boolean(object.isDraft)
        : isSet(object.is_draft)
        ? globalThis.Boolean(object.is_draft)
        : false,
      assignees: globalThis.Array.isArray(object?.assignees)
        ? object.assignees.map((e: any) => globalThis.String(e))
        : [],
      reviewers: globalThis.Array.isArray(object?.reviewers)
        ? object.reviewers.map((e: any) => globalThis.String(e))
        : [],
      updatedAt: isSet(object.updatedAt)
        ? globalThis.String(object.updatedAt)
        : isSet(object.updated_at)
        ? globalThis.String(object.updated_at)
        : "",
      authorAvatarUrl: isSet(object.authorAvatarUrl)
        ? globalThis.String(object.authorAvatarUrl)
        : isSet(object.author_avatar_url)
        ? globalThis.String(object.author_avatar_url)
        : "",
    };
  },

  toJSON(message: GitPullRequest): unknown {
    const obj: any = {};
    if (message.number !== 0) {
      obj.number = Math.round(message.number);
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.state !== "") {
      obj.state = message.state;
    }
    if (message.author !== "") {
      obj.author = message.author;
    }
    if (message.headRef !== "") {
      obj.headRef = message.headRef;
    }
    if (message.baseRef !== "") {
      obj.baseRef = message.baseRef;
    }
    if (message.isDraft !== false) {
      obj.isDraft = message.isDraft;
    }
    if (message.assignees?.length) {
      obj.assignees = message.assignees;
    }
    if (message.reviewers?.length) {
      obj.reviewers = message.reviewers;
    }
    if (message.updatedAt !== "") {
      obj.updatedAt = message.updatedAt;
    }
    if (message.authorAvatarUrl !== "") {
      obj.authorAvatarUrl = message.authorAvatarUrl;
    }
    return obj;
  },

  create(base?: DeepPartial<GitPullRequest>): GitPullRequest {
    return GitPullRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GitPullRequest>): GitPullRequest {
    const message = createBaseGitPullRequest();
    message.number = object.number ?? 0;
    message.title = object.title ?? "";
    message.url = object.url ?? "";
    message.state = object.state ?? "";
    message.author = object.author ?? "";
    message.headRef = object.headRef ?? "";
    message.baseRef = object.baseRef ?? "";
    message.isDraft = object.isDraft ?? false;
    message.assignees = object.assignees?.map((e) => e) || [];
    message.reviewers = object.reviewers?.map((e) => e) || [];
    message.updatedAt = object.updatedAt ?? "";
    message.authorAvatarUrl = object.authorAvatarUrl ?? "";
    return message;
  },
};

function createBaseGitIssue(): GitIssue {
  return {
    number: 0,
    title: "",
    url: "",
    state: "",
    author: "",
    labels: [],
    assignees: [],
    updatedAt: "",
    authorAvatarUrl: "",
  };
}

export const GitIssue: MessageFns<GitIssue> = {
  encode(message: GitIssue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.number !== 0) {
      writer.uint32(8).uint32(message.number);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.url !== "") {
      writer.uint32(26).string(message.url);
    }
    if (message.state !== "") {
      writer.uint32(34).string(message.state);
    }
    if (message.author !== "") {
      writer.uint32(42).string(message.author);
    }
    for (const v of message.labels) {
      writer.uint32(50).string(v!);
    }
    for (const v of message.assignees) {
      writer.uint32(58).string(v!);
    }
    if (message.updatedAt !== "") {
      writer.uint32(66).string(message.updatedAt);
    }
    if (message.authorAvatarUrl !== "") {
      writer.uint32(74).string(message.authorAvatarUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GitIssue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGitIssue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.number = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.state = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.author = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.labels.push(reader.string());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.assignees.push(reader.string());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.updatedAt = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.authorAvatarUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GitIssue {
    return {
      number: isSet(object.number) ? globalThis.Number(object.number) : 0,
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      state: isSet(object.state) ? globalThis.String(object.state) : "",
      author: isSet(object.author) ? globalThis.String(object.author) : "",
      labels: globalThis.Array.isArray(object?.labels) ? object.labels.map((e: any) => globalThis.String(e)) : [],
      assignees: globalThis.Array.isArray(object?.assignees)
        ? object.assignees.map((e: any) => globalThis.String(e))
        : [],
      updatedAt: isSet(object.updatedAt)
        ? globalThis.String(object.updatedAt)
        : isSet(object.updated_at)
        ? globalThis.String(object.updated_at)
        : "",
      authorAvatarUrl: isSet(object.authorAvatarUrl)
        ? globalThis.String(object.authorAvatarUrl)
        : isSet(object.author_avatar_url)
        ? globalThis.String(object.author_avatar_url)
        : "",
    };
  },

  toJSON(message: GitIssue): unknown {
    const obj: any = {};
    if (message.number !== 0) {
      obj.number = Math.round(message.number);
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.state !== "") {
      obj.state = message.state;
    }
    if (message.author !== "") {
      obj.author = message.author;
    }
    if (message.labels?.length) {
      obj.labels = message.labels;
    }
    if (message.assignees?.length) {
      obj.assignees = message.assignees;
    }
    if (message.updatedAt !== "") {
      obj.updatedAt = message.updatedAt;
    }
    if (message.authorAvatarUrl !== "") {
      obj.authorAvatarUrl = message.authorAvatarUrl;
    }
    return obj;
  },

  create(base?: DeepPartial<GitIssue>): GitIssue {
    return GitIssue.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GitIssue>): GitIssue {
    const message = createBaseGitIssue();
    message.number = object.number ?? 0;
    message.title = object.title ?? "";
    message.url = object.url ?? "";
    message.state = object.state ?? "";
    message.author = object.author ?? "";
    message.labels = object.labels?.map((e) => e) || [];
    message.assignees = object.assignees?.map((e) => e) || [];
    message.updatedAt = object.updatedAt ?? "";
    message.authorAvatarUrl = object.authorAvatarUrl ?? "";
    return message;
  },
};

function createBaseOpenTargetSelection(): OpenTargetSelection {
  return { kind: "", relPath: "", lineNumber: 0 };
}

export const OpenTargetSelection: MessageFns<OpenTargetSelection> = {
  encode(message: OpenTargetSelection, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.kind !== "") {
      writer.uint32(10).string(message.kind);
    }
    if (message.relPath !== "") {
      writer.uint32(18).string(message.relPath);
    }
    if (message.lineNumber !== 0) {
      writer.uint32(24).uint32(message.lineNumber);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OpenTargetSelection {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOpenTargetSelection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.kind = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.relPath = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.lineNumber = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OpenTargetSelection {
    return {
      kind: isSet(object.kind) ? globalThis.String(object.kind) : "",
      relPath: isSet(object.relPath)
        ? globalThis.String(object.relPath)
        : isSet(object.rel_path)
        ? globalThis.String(object.rel_path)
        : "",
      lineNumber: isSet(object.lineNumber)
        ? globalThis.Number(object.lineNumber)
        : isSet(object.line_number)
        ? globalThis.Number(object.line_number)
        : 0,
    };
  },

  toJSON(message: OpenTargetSelection): unknown {
    const obj: any = {};
    if (message.kind !== "") {
      obj.kind = message.kind;
    }
    if (message.relPath !== "") {
      obj.relPath = message.relPath;
    }
    if (message.lineNumber !== 0) {
      obj.lineNumber = Math.round(message.lineNumber);
    }
    return obj;
  },

  create(base?: DeepPartial<OpenTargetSelection>): OpenTargetSelection {
    return OpenTargetSelection.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OpenTargetSelection>): OpenTargetSelection {
    const message = createBaseOpenTargetSelection();
    message.kind = object.kind ?? "";
    message.relPath = object.relPath ?? "";
    message.lineNumber = object.lineNumber ?? 0;
    return message;
  },
};

function createBaseProjectConfig(): ProjectConfig {
  return { worktreeSymlinks: [] };
}

export const ProjectConfig: MessageFns<ProjectConfig> = {
  encode(message: ProjectConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.worktreeSymlinks) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProjectConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProjectConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.worktreeSymlinks.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProjectConfig {
    return {
      worktreeSymlinks: globalThis.Array.isArray(object?.worktreeSymlinks)
        ? object.worktreeSymlinks.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.worktree_symlinks)
        ? object.worktree_symlinks.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: ProjectConfig): unknown {
    const obj: any = {};
    if (message.worktreeSymlinks?.length) {
      obj.worktreeSymlinks = message.worktreeSymlinks;
    }
    return obj;
  },

  create(base?: DeepPartial<ProjectConfig>): ProjectConfig {
    return ProjectConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ProjectConfig>): ProjectConfig {
    const message = createBaseProjectConfig();
    message.worktreeSymlinks = object.worktreeSymlinks?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
