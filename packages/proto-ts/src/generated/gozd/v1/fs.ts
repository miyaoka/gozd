// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: gozd/v1/fs.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "gozd.v1";

/**
 * ファイル読み込み（バイナリ含む）。
 *
 * path は dir からの相対パス。dir 範囲外への path traversal は Swift 実装側で
 * 拒否する（issue #310 のステートレス + sandboxing 方針）。
 */
export interface FsReadFileRequest {
  dir: string;
  path: string;
}

export interface FsReadFileResponse {
  data: Uint8Array;
}

/**
 * ディレクトリエントリ列挙。
 *
 * type: "file" / "directory" / "symlink" / "other"。
 * proto3 enum を使うと renderer / Swift 両方で symbol 比較になり読みやすいが、
 * JSON mapping で文字列のまま扱える方が renderer 側のリテラル判定が楽。
 * 第一フェーズは string で進め、必要があれば後で enum に昇格する。
 */
export interface FsReadDirRequest {
  dir: string;
  path: string;
}

export interface FsReadDirEntry {
  name: string;
  type: string;
}

export interface FsReadDirResponse {
  entries: FsReadDirEntry[];
}

function createBaseFsReadFileRequest(): FsReadFileRequest {
  return { dir: "", path: "" };
}

export const FsReadFileRequest: MessageFns<FsReadFileRequest> = {
  encode(message: FsReadFileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dir !== "") {
      writer.uint32(10).string(message.dir);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsReadFileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsReadFileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dir = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsReadFileRequest {
    return {
      dir: isSet(object.dir) ? globalThis.String(object.dir) : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
    };
  },

  toJSON(message: FsReadFileRequest): unknown {
    const obj: any = {};
    if (message.dir !== "") {
      obj.dir = message.dir;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    return obj;
  },

  create(base?: DeepPartial<FsReadFileRequest>): FsReadFileRequest {
    return FsReadFileRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FsReadFileRequest>): FsReadFileRequest {
    const message = createBaseFsReadFileRequest();
    message.dir = object.dir ?? "";
    message.path = object.path ?? "";
    return message;
  },
};

function createBaseFsReadFileResponse(): FsReadFileResponse {
  return { data: new Uint8Array(0) };
}

export const FsReadFileResponse: MessageFns<FsReadFileResponse> = {
  encode(message: FsReadFileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsReadFileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsReadFileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsReadFileResponse {
    return { data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0) };
  },

  toJSON(message: FsReadFileResponse): unknown {
    const obj: any = {};
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<FsReadFileResponse>): FsReadFileResponse {
    return FsReadFileResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FsReadFileResponse>): FsReadFileResponse {
    const message = createBaseFsReadFileResponse();
    message.data = object.data ?? new Uint8Array(0);
    return message;
  },
};

function createBaseFsReadDirRequest(): FsReadDirRequest {
  return { dir: "", path: "" };
}

export const FsReadDirRequest: MessageFns<FsReadDirRequest> = {
  encode(message: FsReadDirRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dir !== "") {
      writer.uint32(10).string(message.dir);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsReadDirRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsReadDirRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dir = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsReadDirRequest {
    return {
      dir: isSet(object.dir) ? globalThis.String(object.dir) : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
    };
  },

  toJSON(message: FsReadDirRequest): unknown {
    const obj: any = {};
    if (message.dir !== "") {
      obj.dir = message.dir;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    return obj;
  },

  create(base?: DeepPartial<FsReadDirRequest>): FsReadDirRequest {
    return FsReadDirRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FsReadDirRequest>): FsReadDirRequest {
    const message = createBaseFsReadDirRequest();
    message.dir = object.dir ?? "";
    message.path = object.path ?? "";
    return message;
  },
};

function createBaseFsReadDirEntry(): FsReadDirEntry {
  return { name: "", type: "" };
}

export const FsReadDirEntry: MessageFns<FsReadDirEntry> = {
  encode(message: FsReadDirEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.type !== "") {
      writer.uint32(18).string(message.type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsReadDirEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsReadDirEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.type = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsReadDirEntry {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
    };
  },

  toJSON(message: FsReadDirEntry): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    return obj;
  },

  create(base?: DeepPartial<FsReadDirEntry>): FsReadDirEntry {
    return FsReadDirEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FsReadDirEntry>): FsReadDirEntry {
    const message = createBaseFsReadDirEntry();
    message.name = object.name ?? "";
    message.type = object.type ?? "";
    return message;
  },
};

function createBaseFsReadDirResponse(): FsReadDirResponse {
  return { entries: [] };
}

export const FsReadDirResponse: MessageFns<FsReadDirResponse> = {
  encode(message: FsReadDirResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.entries) {
      FsReadDirEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsReadDirResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsReadDirResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entries.push(FsReadDirEntry.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsReadDirResponse {
    return {
      entries: globalThis.Array.isArray(object?.entries)
        ? object.entries.map((e: any) => FsReadDirEntry.fromJSON(e))
        : [],
    };
  },

  toJSON(message: FsReadDirResponse): unknown {
    const obj: any = {};
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => FsReadDirEntry.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<FsReadDirResponse>): FsReadDirResponse {
    return FsReadDirResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FsReadDirResponse>): FsReadDirResponse {
    const message = createBaseFsReadDirResponse();
    message.entries = object.entries?.map((e) => FsReadDirEntry.fromPartial(e)) || [];
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
