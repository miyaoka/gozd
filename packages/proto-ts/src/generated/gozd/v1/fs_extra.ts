// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: gozd/v1/fs_extra.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { FileReadResult } from "./common";

export const protobufPackage = "gozd.v1";

/**
 * 絶対パスでのファイル読み取り（dir 外を許可）。プレビュー等で使用。
 * path traversal の責任は呼び出し側に移譲する。
 */
export interface FsReadFileAbsoluteRequest {
  absolutePath: string;
}

export interface FsReadFileAbsoluteResponse {
  result: FileReadResult | undefined;
}

/** fsWriteFile: dir 配下に書き込む。path traversal guard は server 側。 */
export interface FsWriteFileRequest {
  dir: string;
  path: string;
  data: Uint8Array;
}

export interface FsWriteFileResponse {
}

/** fsStat: ファイル / ディレクトリの存在確認 + 種別取得 */
export interface FsStatRequest {
  dir: string;
  path: string;
}

export interface FsStatResponse {
  exists: boolean;
  /** "file" / "directory" / "symlink" */
  type: string;
  size: number;
  /** ISO 8601 */
  modifiedAt: string;
}

function createBaseFsReadFileAbsoluteRequest(): FsReadFileAbsoluteRequest {
  return { absolutePath: "" };
}

export const FsReadFileAbsoluteRequest: MessageFns<FsReadFileAbsoluteRequest> = {
  encode(message: FsReadFileAbsoluteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.absolutePath !== "") {
      writer.uint32(10).string(message.absolutePath);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsReadFileAbsoluteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsReadFileAbsoluteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.absolutePath = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsReadFileAbsoluteRequest {
    return {
      absolutePath: isSet(object.absolutePath)
        ? globalThis.String(object.absolutePath)
        : isSet(object.absolute_path)
        ? globalThis.String(object.absolute_path)
        : "",
    };
  },

  toJSON(message: FsReadFileAbsoluteRequest): unknown {
    const obj: any = {};
    if (message.absolutePath !== "") {
      obj.absolutePath = message.absolutePath;
    }
    return obj;
  },

  create(base?: DeepPartial<FsReadFileAbsoluteRequest>): FsReadFileAbsoluteRequest {
    return FsReadFileAbsoluteRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FsReadFileAbsoluteRequest>): FsReadFileAbsoluteRequest {
    const message = createBaseFsReadFileAbsoluteRequest();
    message.absolutePath = object.absolutePath ?? "";
    return message;
  },
};

function createBaseFsReadFileAbsoluteResponse(): FsReadFileAbsoluteResponse {
  return { result: undefined };
}

export const FsReadFileAbsoluteResponse: MessageFns<FsReadFileAbsoluteResponse> = {
  encode(message: FsReadFileAbsoluteResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.result !== undefined) {
      FileReadResult.encode(message.result, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsReadFileAbsoluteResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsReadFileAbsoluteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.result = FileReadResult.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsReadFileAbsoluteResponse {
    return { result: isSet(object.result) ? FileReadResult.fromJSON(object.result) : undefined };
  },

  toJSON(message: FsReadFileAbsoluteResponse): unknown {
    const obj: any = {};
    if (message.result !== undefined) {
      obj.result = FileReadResult.toJSON(message.result);
    }
    return obj;
  },

  create(base?: DeepPartial<FsReadFileAbsoluteResponse>): FsReadFileAbsoluteResponse {
    return FsReadFileAbsoluteResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FsReadFileAbsoluteResponse>): FsReadFileAbsoluteResponse {
    const message = createBaseFsReadFileAbsoluteResponse();
    message.result = (object.result !== undefined && object.result !== null)
      ? FileReadResult.fromPartial(object.result)
      : undefined;
    return message;
  },
};

function createBaseFsWriteFileRequest(): FsWriteFileRequest {
  return { dir: "", path: "", data: new Uint8Array(0) };
}

export const FsWriteFileRequest: MessageFns<FsWriteFileRequest> = {
  encode(message: FsWriteFileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dir !== "") {
      writer.uint32(10).string(message.dir);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    if (message.data.length !== 0) {
      writer.uint32(26).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsWriteFileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsWriteFileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dir = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsWriteFileRequest {
    return {
      dir: isSet(object.dir) ? globalThis.String(object.dir) : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
    };
  },

  toJSON(message: FsWriteFileRequest): unknown {
    const obj: any = {};
    if (message.dir !== "") {
      obj.dir = message.dir;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<FsWriteFileRequest>): FsWriteFileRequest {
    return FsWriteFileRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FsWriteFileRequest>): FsWriteFileRequest {
    const message = createBaseFsWriteFileRequest();
    message.dir = object.dir ?? "";
    message.path = object.path ?? "";
    message.data = object.data ?? new Uint8Array(0);
    return message;
  },
};

function createBaseFsWriteFileResponse(): FsWriteFileResponse {
  return {};
}

export const FsWriteFileResponse: MessageFns<FsWriteFileResponse> = {
  encode(_: FsWriteFileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsWriteFileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsWriteFileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): FsWriteFileResponse {
    return {};
  },

  toJSON(_: FsWriteFileResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<FsWriteFileResponse>): FsWriteFileResponse {
    return FsWriteFileResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<FsWriteFileResponse>): FsWriteFileResponse {
    const message = createBaseFsWriteFileResponse();
    return message;
  },
};

function createBaseFsStatRequest(): FsStatRequest {
  return { dir: "", path: "" };
}

export const FsStatRequest: MessageFns<FsStatRequest> = {
  encode(message: FsStatRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dir !== "") {
      writer.uint32(10).string(message.dir);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsStatRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsStatRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dir = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsStatRequest {
    return {
      dir: isSet(object.dir) ? globalThis.String(object.dir) : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
    };
  },

  toJSON(message: FsStatRequest): unknown {
    const obj: any = {};
    if (message.dir !== "") {
      obj.dir = message.dir;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    return obj;
  },

  create(base?: DeepPartial<FsStatRequest>): FsStatRequest {
    return FsStatRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FsStatRequest>): FsStatRequest {
    const message = createBaseFsStatRequest();
    message.dir = object.dir ?? "";
    message.path = object.path ?? "";
    return message;
  },
};

function createBaseFsStatResponse(): FsStatResponse {
  return { exists: false, type: "", size: 0, modifiedAt: "" };
}

export const FsStatResponse: MessageFns<FsStatResponse> = {
  encode(message: FsStatResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exists !== false) {
      writer.uint32(8).bool(message.exists);
    }
    if (message.type !== "") {
      writer.uint32(18).string(message.type);
    }
    if (message.size !== 0) {
      writer.uint32(24).uint64(message.size);
    }
    if (message.modifiedAt !== "") {
      writer.uint32(34).string(message.modifiedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsStatResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsStatResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.exists = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.size = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.modifiedAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsStatResponse {
    return {
      exists: isSet(object.exists) ? globalThis.Boolean(object.exists) : false,
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      size: isSet(object.size) ? globalThis.Number(object.size) : 0,
      modifiedAt: isSet(object.modifiedAt)
        ? globalThis.String(object.modifiedAt)
        : isSet(object.modified_at)
        ? globalThis.String(object.modified_at)
        : "",
    };
  },

  toJSON(message: FsStatResponse): unknown {
    const obj: any = {};
    if (message.exists !== false) {
      obj.exists = message.exists;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.size !== 0) {
      obj.size = Math.round(message.size);
    }
    if (message.modifiedAt !== "") {
      obj.modifiedAt = message.modifiedAt;
    }
    return obj;
  },

  create(base?: DeepPartial<FsStatResponse>): FsStatResponse {
    return FsStatResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FsStatResponse>): FsStatResponse {
    const message = createBaseFsStatResponse();
    message.exists = object.exists ?? false;
    message.type = object.type ?? "";
    message.size = object.size ?? 0;
    message.modifiedAt = object.modifiedAt ?? "";
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
