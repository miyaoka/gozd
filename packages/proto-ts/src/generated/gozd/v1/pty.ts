// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: gozd/v1/pty.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "gozd.v1";

/**
 * PTY 子プロセスを spawn して PTY master fd を確立する。
 * 戻り値 pty_id は同一 SocketServer / WebView セッション内で有効な不透明 ID。
 */
export interface PtySpawnRequest {
  dir: string;
  executable: string;
  args: string[];
  env: { [key: string]: string };
  rows: number;
  cols: number;
}

export interface PtySpawnRequest_EnvEntry {
  key: string;
  value: string;
}

export interface PtySpawnResponse {
  ptyId: number;
}

/** PTY master fd への書き込み（renderer → 子プロセスのキー入力等）。 */
export interface PtyWriteRequest {
  ptyId: number;
  data: Uint8Array;
}

export interface PtyWriteResponse {
}

/** terminal サイズの変更を子プロセスに通知（xterm.js のリサイズ連動）。 */
export interface PtyResizeRequest {
  ptyId: number;
  rows: number;
  cols: number;
}

export interface PtyResizeResponse {
}

/** 子プロセスに SIGHUP を送る。 */
export interface PtyKillRequest {
  ptyId: number;
}

export interface PtyKillResponse {
}

/** renderer 側に push される PTY イベント（Swift → JS、callJavaScript で配送）。 */
export interface PtyDataEvent {
  ptyId: number;
  data: Uint8Array;
}

export interface PtyExitEvent {
  ptyId: number;
  /** "exited" / "signaled" / "stopped" */
  reason: string;
  /** exited 時の exit code、signaled 時の signal 番号、stopped 時は 0 */
  code: number;
  /** signaled 時のみ意味を持つ */
  coreDumped: boolean;
}

function createBasePtySpawnRequest(): PtySpawnRequest {
  return { dir: "", executable: "", args: [], env: {}, rows: 0, cols: 0 };
}

export const PtySpawnRequest: MessageFns<PtySpawnRequest> = {
  encode(message: PtySpawnRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dir !== "") {
      writer.uint32(10).string(message.dir);
    }
    if (message.executable !== "") {
      writer.uint32(18).string(message.executable);
    }
    for (const v of message.args) {
      writer.uint32(26).string(v!);
    }
    globalThis.Object.entries(message.env).forEach(([key, value]: [string, string]) => {
      PtySpawnRequest_EnvEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    if (message.rows !== 0) {
      writer.uint32(40).uint32(message.rows);
    }
    if (message.cols !== 0) {
      writer.uint32(48).uint32(message.cols);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PtySpawnRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePtySpawnRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dir = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.executable = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.args.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = PtySpawnRequest_EnvEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.env[entry4.key] = entry4.value;
          }
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.rows = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.cols = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PtySpawnRequest {
    return {
      dir: isSet(object.dir) ? globalThis.String(object.dir) : "",
      executable: isSet(object.executable) ? globalThis.String(object.executable) : "",
      args: globalThis.Array.isArray(object?.args) ? object.args.map((e: any) => globalThis.String(e)) : [],
      env: isObject(object.env)
        ? (globalThis.Object.entries(object.env) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      rows: isSet(object.rows) ? globalThis.Number(object.rows) : 0,
      cols: isSet(object.cols) ? globalThis.Number(object.cols) : 0,
    };
  },

  toJSON(message: PtySpawnRequest): unknown {
    const obj: any = {};
    if (message.dir !== "") {
      obj.dir = message.dir;
    }
    if (message.executable !== "") {
      obj.executable = message.executable;
    }
    if (message.args?.length) {
      obj.args = message.args;
    }
    if (message.env) {
      const entries = globalThis.Object.entries(message.env) as [string, string][];
      if (entries.length > 0) {
        obj.env = {};
        entries.forEach(([k, v]) => {
          obj.env[k] = v;
        });
      }
    }
    if (message.rows !== 0) {
      obj.rows = Math.round(message.rows);
    }
    if (message.cols !== 0) {
      obj.cols = Math.round(message.cols);
    }
    return obj;
  },

  create(base?: DeepPartial<PtySpawnRequest>): PtySpawnRequest {
    return PtySpawnRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PtySpawnRequest>): PtySpawnRequest {
    const message = createBasePtySpawnRequest();
    message.dir = object.dir ?? "";
    message.executable = object.executable ?? "";
    message.args = object.args?.map((e) => e) || [];
    message.env = (globalThis.Object.entries(object.env ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.rows = object.rows ?? 0;
    message.cols = object.cols ?? 0;
    return message;
  },
};

function createBasePtySpawnRequest_EnvEntry(): PtySpawnRequest_EnvEntry {
  return { key: "", value: "" };
}

export const PtySpawnRequest_EnvEntry: MessageFns<PtySpawnRequest_EnvEntry> = {
  encode(message: PtySpawnRequest_EnvEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PtySpawnRequest_EnvEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePtySpawnRequest_EnvEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PtySpawnRequest_EnvEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: PtySpawnRequest_EnvEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<PtySpawnRequest_EnvEntry>): PtySpawnRequest_EnvEntry {
    return PtySpawnRequest_EnvEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PtySpawnRequest_EnvEntry>): PtySpawnRequest_EnvEntry {
    const message = createBasePtySpawnRequest_EnvEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBasePtySpawnResponse(): PtySpawnResponse {
  return { ptyId: 0 };
}

export const PtySpawnResponse: MessageFns<PtySpawnResponse> = {
  encode(message: PtySpawnResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ptyId !== 0) {
      writer.uint32(8).uint32(message.ptyId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PtySpawnResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePtySpawnResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ptyId = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PtySpawnResponse {
    return {
      ptyId: isSet(object.ptyId)
        ? globalThis.Number(object.ptyId)
        : isSet(object.pty_id)
        ? globalThis.Number(object.pty_id)
        : 0,
    };
  },

  toJSON(message: PtySpawnResponse): unknown {
    const obj: any = {};
    if (message.ptyId !== 0) {
      obj.ptyId = Math.round(message.ptyId);
    }
    return obj;
  },

  create(base?: DeepPartial<PtySpawnResponse>): PtySpawnResponse {
    return PtySpawnResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PtySpawnResponse>): PtySpawnResponse {
    const message = createBasePtySpawnResponse();
    message.ptyId = object.ptyId ?? 0;
    return message;
  },
};

function createBasePtyWriteRequest(): PtyWriteRequest {
  return { ptyId: 0, data: new Uint8Array(0) };
}

export const PtyWriteRequest: MessageFns<PtyWriteRequest> = {
  encode(message: PtyWriteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ptyId !== 0) {
      writer.uint32(8).uint32(message.ptyId);
    }
    if (message.data.length !== 0) {
      writer.uint32(18).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PtyWriteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePtyWriteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ptyId = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PtyWriteRequest {
    return {
      ptyId: isSet(object.ptyId)
        ? globalThis.Number(object.ptyId)
        : isSet(object.pty_id)
        ? globalThis.Number(object.pty_id)
        : 0,
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
    };
  },

  toJSON(message: PtyWriteRequest): unknown {
    const obj: any = {};
    if (message.ptyId !== 0) {
      obj.ptyId = Math.round(message.ptyId);
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<PtyWriteRequest>): PtyWriteRequest {
    return PtyWriteRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PtyWriteRequest>): PtyWriteRequest {
    const message = createBasePtyWriteRequest();
    message.ptyId = object.ptyId ?? 0;
    message.data = object.data ?? new Uint8Array(0);
    return message;
  },
};

function createBasePtyWriteResponse(): PtyWriteResponse {
  return {};
}

export const PtyWriteResponse: MessageFns<PtyWriteResponse> = {
  encode(_: PtyWriteResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PtyWriteResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePtyWriteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PtyWriteResponse {
    return {};
  },

  toJSON(_: PtyWriteResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<PtyWriteResponse>): PtyWriteResponse {
    return PtyWriteResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<PtyWriteResponse>): PtyWriteResponse {
    const message = createBasePtyWriteResponse();
    return message;
  },
};

function createBasePtyResizeRequest(): PtyResizeRequest {
  return { ptyId: 0, rows: 0, cols: 0 };
}

export const PtyResizeRequest: MessageFns<PtyResizeRequest> = {
  encode(message: PtyResizeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ptyId !== 0) {
      writer.uint32(8).uint32(message.ptyId);
    }
    if (message.rows !== 0) {
      writer.uint32(16).uint32(message.rows);
    }
    if (message.cols !== 0) {
      writer.uint32(24).uint32(message.cols);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PtyResizeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePtyResizeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ptyId = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.rows = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.cols = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PtyResizeRequest {
    return {
      ptyId: isSet(object.ptyId)
        ? globalThis.Number(object.ptyId)
        : isSet(object.pty_id)
        ? globalThis.Number(object.pty_id)
        : 0,
      rows: isSet(object.rows) ? globalThis.Number(object.rows) : 0,
      cols: isSet(object.cols) ? globalThis.Number(object.cols) : 0,
    };
  },

  toJSON(message: PtyResizeRequest): unknown {
    const obj: any = {};
    if (message.ptyId !== 0) {
      obj.ptyId = Math.round(message.ptyId);
    }
    if (message.rows !== 0) {
      obj.rows = Math.round(message.rows);
    }
    if (message.cols !== 0) {
      obj.cols = Math.round(message.cols);
    }
    return obj;
  },

  create(base?: DeepPartial<PtyResizeRequest>): PtyResizeRequest {
    return PtyResizeRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PtyResizeRequest>): PtyResizeRequest {
    const message = createBasePtyResizeRequest();
    message.ptyId = object.ptyId ?? 0;
    message.rows = object.rows ?? 0;
    message.cols = object.cols ?? 0;
    return message;
  },
};

function createBasePtyResizeResponse(): PtyResizeResponse {
  return {};
}

export const PtyResizeResponse: MessageFns<PtyResizeResponse> = {
  encode(_: PtyResizeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PtyResizeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePtyResizeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PtyResizeResponse {
    return {};
  },

  toJSON(_: PtyResizeResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<PtyResizeResponse>): PtyResizeResponse {
    return PtyResizeResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<PtyResizeResponse>): PtyResizeResponse {
    const message = createBasePtyResizeResponse();
    return message;
  },
};

function createBasePtyKillRequest(): PtyKillRequest {
  return { ptyId: 0 };
}

export const PtyKillRequest: MessageFns<PtyKillRequest> = {
  encode(message: PtyKillRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ptyId !== 0) {
      writer.uint32(8).uint32(message.ptyId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PtyKillRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePtyKillRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ptyId = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PtyKillRequest {
    return {
      ptyId: isSet(object.ptyId)
        ? globalThis.Number(object.ptyId)
        : isSet(object.pty_id)
        ? globalThis.Number(object.pty_id)
        : 0,
    };
  },

  toJSON(message: PtyKillRequest): unknown {
    const obj: any = {};
    if (message.ptyId !== 0) {
      obj.ptyId = Math.round(message.ptyId);
    }
    return obj;
  },

  create(base?: DeepPartial<PtyKillRequest>): PtyKillRequest {
    return PtyKillRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PtyKillRequest>): PtyKillRequest {
    const message = createBasePtyKillRequest();
    message.ptyId = object.ptyId ?? 0;
    return message;
  },
};

function createBasePtyKillResponse(): PtyKillResponse {
  return {};
}

export const PtyKillResponse: MessageFns<PtyKillResponse> = {
  encode(_: PtyKillResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PtyKillResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePtyKillResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PtyKillResponse {
    return {};
  },

  toJSON(_: PtyKillResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<PtyKillResponse>): PtyKillResponse {
    return PtyKillResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<PtyKillResponse>): PtyKillResponse {
    const message = createBasePtyKillResponse();
    return message;
  },
};

function createBasePtyDataEvent(): PtyDataEvent {
  return { ptyId: 0, data: new Uint8Array(0) };
}

export const PtyDataEvent: MessageFns<PtyDataEvent> = {
  encode(message: PtyDataEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ptyId !== 0) {
      writer.uint32(8).uint32(message.ptyId);
    }
    if (message.data.length !== 0) {
      writer.uint32(18).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PtyDataEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePtyDataEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ptyId = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PtyDataEvent {
    return {
      ptyId: isSet(object.ptyId)
        ? globalThis.Number(object.ptyId)
        : isSet(object.pty_id)
        ? globalThis.Number(object.pty_id)
        : 0,
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
    };
  },

  toJSON(message: PtyDataEvent): unknown {
    const obj: any = {};
    if (message.ptyId !== 0) {
      obj.ptyId = Math.round(message.ptyId);
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<PtyDataEvent>): PtyDataEvent {
    return PtyDataEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PtyDataEvent>): PtyDataEvent {
    const message = createBasePtyDataEvent();
    message.ptyId = object.ptyId ?? 0;
    message.data = object.data ?? new Uint8Array(0);
    return message;
  },
};

function createBasePtyExitEvent(): PtyExitEvent {
  return { ptyId: 0, reason: "", code: 0, coreDumped: false };
}

export const PtyExitEvent: MessageFns<PtyExitEvent> = {
  encode(message: PtyExitEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ptyId !== 0) {
      writer.uint32(8).uint32(message.ptyId);
    }
    if (message.reason !== "") {
      writer.uint32(18).string(message.reason);
    }
    if (message.code !== 0) {
      writer.uint32(24).int32(message.code);
    }
    if (message.coreDumped !== false) {
      writer.uint32(32).bool(message.coreDumped);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PtyExitEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePtyExitEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ptyId = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.code = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.coreDumped = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PtyExitEvent {
    return {
      ptyId: isSet(object.ptyId)
        ? globalThis.Number(object.ptyId)
        : isSet(object.pty_id)
        ? globalThis.Number(object.pty_id)
        : 0,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      code: isSet(object.code) ? globalThis.Number(object.code) : 0,
      coreDumped: isSet(object.coreDumped)
        ? globalThis.Boolean(object.coreDumped)
        : isSet(object.core_dumped)
        ? globalThis.Boolean(object.core_dumped)
        : false,
    };
  },

  toJSON(message: PtyExitEvent): unknown {
    const obj: any = {};
    if (message.ptyId !== 0) {
      obj.ptyId = Math.round(message.ptyId);
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.code !== 0) {
      obj.code = Math.round(message.code);
    }
    if (message.coreDumped !== false) {
      obj.coreDumped = message.coreDumped;
    }
    return obj;
  },

  create(base?: DeepPartial<PtyExitEvent>): PtyExitEvent {
    return PtyExitEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PtyExitEvent>): PtyExitEvent {
    const message = createBasePtyExitEvent();
    message.ptyId = object.ptyId ?? 0;
    message.reason = object.reason ?? "";
    message.code = object.code ?? 0;
    message.coreDumped = object.coreDumped ?? false;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
