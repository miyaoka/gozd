// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: gozd/v1/open_target.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "gozd.v1";

/**
 * NSOpenPanel をネイティブで開いてユーザーに dir を選ばせる。
 * 選択後、内部で openTarget callback を呼んで gozdOpen を push する。
 * ユーザーがキャンセルした場合は何もしない。
 */
export interface PickAndOpenRequest {
}

export interface PickAndOpenResponse {
}

function createBasePickAndOpenRequest(): PickAndOpenRequest {
  return {};
}

export const PickAndOpenRequest: MessageFns<PickAndOpenRequest> = {
  encode(_: PickAndOpenRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PickAndOpenRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePickAndOpenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PickAndOpenRequest {
    return {};
  },

  toJSON(_: PickAndOpenRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<PickAndOpenRequest>): PickAndOpenRequest {
    return PickAndOpenRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<PickAndOpenRequest>): PickAndOpenRequest {
    const message = createBasePickAndOpenRequest();
    return message;
  },
};

function createBasePickAndOpenResponse(): PickAndOpenResponse {
  return {};
}

export const PickAndOpenResponse: MessageFns<PickAndOpenResponse> = {
  encode(_: PickAndOpenResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PickAndOpenResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePickAndOpenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PickAndOpenResponse {
    return {};
  },

  toJSON(_: PickAndOpenResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<PickAndOpenResponse>): PickAndOpenResponse {
    return PickAndOpenResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<PickAndOpenResponse>): PickAndOpenResponse {
    const message = createBasePickAndOpenResponse();
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
