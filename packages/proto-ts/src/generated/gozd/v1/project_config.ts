// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: gozd/v1/project_config.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { ProjectConfig } from "./common";

export const protobufPackage = "gozd.v1";

export interface ProjectConfigLoadRequest {
  dir: string;
}

export interface ProjectConfigLoadResponse {
  config: ProjectConfig | undefined;
}

export interface ProjectConfigSaveRequest {
  dir: string;
  config: ProjectConfig | undefined;
}

export interface ProjectConfigSaveResponse {
}

function createBaseProjectConfigLoadRequest(): ProjectConfigLoadRequest {
  return { dir: "" };
}

export const ProjectConfigLoadRequest: MessageFns<ProjectConfigLoadRequest> = {
  encode(message: ProjectConfigLoadRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dir !== "") {
      writer.uint32(10).string(message.dir);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProjectConfigLoadRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProjectConfigLoadRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dir = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProjectConfigLoadRequest {
    return { dir: isSet(object.dir) ? globalThis.String(object.dir) : "" };
  },

  toJSON(message: ProjectConfigLoadRequest): unknown {
    const obj: any = {};
    if (message.dir !== "") {
      obj.dir = message.dir;
    }
    return obj;
  },

  create(base?: DeepPartial<ProjectConfigLoadRequest>): ProjectConfigLoadRequest {
    return ProjectConfigLoadRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ProjectConfigLoadRequest>): ProjectConfigLoadRequest {
    const message = createBaseProjectConfigLoadRequest();
    message.dir = object.dir ?? "";
    return message;
  },
};

function createBaseProjectConfigLoadResponse(): ProjectConfigLoadResponse {
  return { config: undefined };
}

export const ProjectConfigLoadResponse: MessageFns<ProjectConfigLoadResponse> = {
  encode(message: ProjectConfigLoadResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.config !== undefined) {
      ProjectConfig.encode(message.config, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProjectConfigLoadResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProjectConfigLoadResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.config = ProjectConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProjectConfigLoadResponse {
    return { config: isSet(object.config) ? ProjectConfig.fromJSON(object.config) : undefined };
  },

  toJSON(message: ProjectConfigLoadResponse): unknown {
    const obj: any = {};
    if (message.config !== undefined) {
      obj.config = ProjectConfig.toJSON(message.config);
    }
    return obj;
  },

  create(base?: DeepPartial<ProjectConfigLoadResponse>): ProjectConfigLoadResponse {
    return ProjectConfigLoadResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ProjectConfigLoadResponse>): ProjectConfigLoadResponse {
    const message = createBaseProjectConfigLoadResponse();
    message.config = (object.config !== undefined && object.config !== null)
      ? ProjectConfig.fromPartial(object.config)
      : undefined;
    return message;
  },
};

function createBaseProjectConfigSaveRequest(): ProjectConfigSaveRequest {
  return { dir: "", config: undefined };
}

export const ProjectConfigSaveRequest: MessageFns<ProjectConfigSaveRequest> = {
  encode(message: ProjectConfigSaveRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dir !== "") {
      writer.uint32(10).string(message.dir);
    }
    if (message.config !== undefined) {
      ProjectConfig.encode(message.config, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProjectConfigSaveRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProjectConfigSaveRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dir = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.config = ProjectConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProjectConfigSaveRequest {
    return {
      dir: isSet(object.dir) ? globalThis.String(object.dir) : "",
      config: isSet(object.config) ? ProjectConfig.fromJSON(object.config) : undefined,
    };
  },

  toJSON(message: ProjectConfigSaveRequest): unknown {
    const obj: any = {};
    if (message.dir !== "") {
      obj.dir = message.dir;
    }
    if (message.config !== undefined) {
      obj.config = ProjectConfig.toJSON(message.config);
    }
    return obj;
  },

  create(base?: DeepPartial<ProjectConfigSaveRequest>): ProjectConfigSaveRequest {
    return ProjectConfigSaveRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ProjectConfigSaveRequest>): ProjectConfigSaveRequest {
    const message = createBaseProjectConfigSaveRequest();
    message.dir = object.dir ?? "";
    message.config = (object.config !== undefined && object.config !== null)
      ? ProjectConfig.fromPartial(object.config)
      : undefined;
    return message;
  },
};

function createBaseProjectConfigSaveResponse(): ProjectConfigSaveResponse {
  return {};
}

export const ProjectConfigSaveResponse: MessageFns<ProjectConfigSaveResponse> = {
  encode(_: ProjectConfigSaveResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProjectConfigSaveResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProjectConfigSaveResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ProjectConfigSaveResponse {
    return {};
  },

  toJSON(_: ProjectConfigSaveResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ProjectConfigSaveResponse>): ProjectConfigSaveResponse {
    return ProjectConfigSaveResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ProjectConfigSaveResponse>): ProjectConfigSaveResponse {
    const message = createBaseProjectConfigSaveResponse();
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
