// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: gozd/v1/git_status.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { UpstreamStatus } from "./common";

export const protobufPackage = "gozd.v1";

/**
 * `git status --porcelain=v1 -z` 相当のファイル状態を取得する。
 *
 * issue #310 の方針: 全 RPC は明示的に `dir` を受け取り、
 * サーバー側で `currentDir` を持たないステートレス API として動作する。
 */
export interface GitStatusRequest {
  /** 対象 worktree の絶対パス。 */
  dir: string;
}

export interface GitStatusResponse {
  /**
   * ファイル相対パス → porcelain v1 の XY ステータスコード（例: " M", "??", "M ", "R "）。
   * 値は常に長さ 2 の文字列。1 文字目 = index 状態、2 文字目 = working tree 状態。
   */
  entries: { [key: string]: string };
  /** upstream に対する差分。未設定なら不在。 */
  upstream?: UpstreamStatus | undefined;
}

export interface GitStatusResponse_EntriesEntry {
  key: string;
  value: string;
}

function createBaseGitStatusRequest(): GitStatusRequest {
  return { dir: "" };
}

export const GitStatusRequest: MessageFns<GitStatusRequest> = {
  encode(message: GitStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dir !== "") {
      writer.uint32(10).string(message.dir);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GitStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGitStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dir = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GitStatusRequest {
    return { dir: isSet(object.dir) ? globalThis.String(object.dir) : "" };
  },

  toJSON(message: GitStatusRequest): unknown {
    const obj: any = {};
    if (message.dir !== "") {
      obj.dir = message.dir;
    }
    return obj;
  },

  create(base?: DeepPartial<GitStatusRequest>): GitStatusRequest {
    return GitStatusRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GitStatusRequest>): GitStatusRequest {
    const message = createBaseGitStatusRequest();
    message.dir = object.dir ?? "";
    return message;
  },
};

function createBaseGitStatusResponse(): GitStatusResponse {
  return { entries: {}, upstream: undefined };
}

export const GitStatusResponse: MessageFns<GitStatusResponse> = {
  encode(message: GitStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    globalThis.Object.entries(message.entries).forEach(([key, value]: [string, string]) => {
      GitStatusResponse_EntriesEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    if (message.upstream !== undefined) {
      UpstreamStatus.encode(message.upstream, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GitStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGitStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = GitStatusResponse_EntriesEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.entries[entry1.key] = entry1.value;
          }
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.upstream = UpstreamStatus.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GitStatusResponse {
    return {
      entries: isObject(object.entries)
        ? (globalThis.Object.entries(object.entries) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      upstream: isSet(object.upstream) ? UpstreamStatus.fromJSON(object.upstream) : undefined,
    };
  },

  toJSON(message: GitStatusResponse): unknown {
    const obj: any = {};
    if (message.entries) {
      const entries = globalThis.Object.entries(message.entries) as [string, string][];
      if (entries.length > 0) {
        obj.entries = {};
        entries.forEach(([k, v]) => {
          obj.entries[k] = v;
        });
      }
    }
    if (message.upstream !== undefined) {
      obj.upstream = UpstreamStatus.toJSON(message.upstream);
    }
    return obj;
  },

  create(base?: DeepPartial<GitStatusResponse>): GitStatusResponse {
    return GitStatusResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GitStatusResponse>): GitStatusResponse {
    const message = createBaseGitStatusResponse();
    message.entries = (globalThis.Object.entries(object.entries ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.upstream = (object.upstream !== undefined && object.upstream !== null)
      ? UpstreamStatus.fromPartial(object.upstream)
      : undefined;
    return message;
  },
};

function createBaseGitStatusResponse_EntriesEntry(): GitStatusResponse_EntriesEntry {
  return { key: "", value: "" };
}

export const GitStatusResponse_EntriesEntry: MessageFns<GitStatusResponse_EntriesEntry> = {
  encode(message: GitStatusResponse_EntriesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GitStatusResponse_EntriesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGitStatusResponse_EntriesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GitStatusResponse_EntriesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: GitStatusResponse_EntriesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<GitStatusResponse_EntriesEntry>): GitStatusResponse_EntriesEntry {
    return GitStatusResponse_EntriesEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GitStatusResponse_EntriesEntry>): GitStatusResponse_EntriesEntry {
    const message = createBaseGitStatusResponse_EntriesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
